---
phase: 01-marketing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - components.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - src/config/site.ts
  - src/components/ui/
  - public/founder-placeholder.svg
autonomous: true
requirements: [SITE-05, SITE-06]
must_haves:
  truths:
    - "Project builds cleanly with `next build` (Turbopack, no font/token errors)"
    - "shadcn is initialized with Radix primitives and neutral base (components.json shows baseColor neutral, style starting radix-)"
    - "Headings render in Fraunces and body in IBM Plex Sans — no Geist/Inter anywhere"
    - "The 60/30/10 UI-SPEC palette is the theme source in globals.css"
    - "All founder-identity values resolve through a single typed siteConfig module"
  artifacts:
    - path: "src/config/site.ts"
      provides: "Single typed source of truth for name/founderName/founderEmail/region/domain/calLink/calNamespace (D-06)"
      contains: "export const siteConfig"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme + UI-SPEC 60/30/10 color tokens and restrained radius"
      contains: "#1F6E4A"
    - path: "src/app/layout.tsx"
      provides: "Fraunces + IBM Plex Sans font wiring via next/font/google"
      contains: "IBM_Plex_Sans"
    - path: "components.json"
      provides: "shadcn config proving Radix + neutral"
      contains: "\"baseColor\": \"neutral\""
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "font CSS variables (--font-fraunces / --font-ibm-plex-sans) mapped into @theme"
      pattern: "--font-(fraunces|ibm-plex-sans)"
---

<objective>
Scaffold the Next.js 16 project and establish the locked design system so every downstream page inherits the correct typography, color, and identity source of truth. This is the base of the Walking Skeleton — nothing renders correctly until this exists.

Purpose: Convert the empty repo into a buildable, correctly-themed Next.js app that satisfies the SITE-06 "not-templated" design contract at the token level and the SITE-05 single-identity-source requirement (D-06).
Output: A themed, buildable scaffold with `src/config/site.ts`, overridden `globals.css`, Fraunces/IBM Plex Sans fonts, and shadcn primitives installed.
</objective>

## Phase Goal

**As a** skeptical SMB owner, **I want to** browse a credible, custom-designed marketing site and book a free audit call, **so that** I feel confident this consultant can recover real time and money for my business.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-marketing-foundation/01-UI-SPEC.md
@.planning/phases/01-marketing-foundation/01-RESEARCH.md
@.planning/phases/01-marketing-foundation/01-CONTEXT.md
@.planning/phases/01-marketing-foundation/SKELETON.md

<interfaces>
<!-- The typed siteConfig shape that ALL later plans import. Define exactly these keys. -->
<!-- Placeholder values are NEEDS-FOUNDER launch blockers (D-04/D-05/D-07/D-13) — never invent real values. -->

src/config/site.ts exports:
  siteConfig = {
    name: "AI Deployed",
    founderName: string,        // NEEDS-FOUNDER (D-13) — placeholder "Annie" acceptable in dev, flag as launch blocker
    founderEmail: string,       // NEEDS-FOUNDER (D-04/D-05) — obvious placeholder, launch blocker
    region: string,             // NEEDS-FOUNDER (D-13)
    domain: string,             // NEEDS-FOUNDER (D-04) — until then, Netlify subdomain
    calLink: string,            // NEEDS-FOUNDER (D-01) — "username/free-audit-call" once event created
    calNamespace: "free-audit-call"
  } as const
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 + shadcn (corrected CLI) + Cal.com package</name>
  <read_first>
    - CLAUDE.md (locked stack versions + "What NOT to Use" table)
    - .planning/phases/01-marketing-foundation/01-RESEARCH.md — "Standard Stack", "Package Legitimacy Audit", and Critical Pitfall #1 (the stale shadcn command)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Phase Component Inventory" (which primitives to add)
  </read_first>
  <action>
    Scaffold into the current repo (already contains .git, CLAUDE.md, .planning/) with `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --turbopack`. If create-next-app refuses due to existing files, scaffold in a temp dir and move generated files in without clobbering CLAUDE.md or .planning/.

    Initialize shadcn with the CORRECTED command from RESEARCH Critical Pitfall #1: `npx shadcn@latest init -b radix` (NOT the UI-SPEC's stale `-b neutral -s new-york`). Select the "Nova" preset (or Custom) — treat its output as a starting point to override later. Do NOT pass `--preset custom` (invalid). Do NOT reference or install `@calcom/react-widget` (does not exist — 404).

    Install the Cal.com embed: `npm install @calcom/embed-react` (version 1.5.3, the only real package). Add the UI-SPEC primitives: `npx shadcn@latest add button card badge navigation-menu sheet separator skeleton`.

    Confirm `next.config.ts` does NOT set `output: 'export'` (would break Phase 2/3 Route Handlers — RESEARCH anti-pattern). Do NOT create any `src/app/api/` directory or introduce any secret/env var in this phase.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q '"baseColor": "neutral"' components.json && grep -q '@calcom/embed-react' package.json && ! grep -rq "output.*export" next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` contains `"baseColor": "neutral"` and a `"style"` value beginning with `radix-`
    - `package.json` dependencies include `@calcom/embed-react` and NOT `@calcom/react-widget`
    - `src/components/ui/` contains button.tsx, card.tsx, badge.tsx, navigation-menu.tsx, sheet.tsx, separator.tsx, skeleton.tsx
    - `next.config.ts` does not contain `output: 'export'`
    - `npx next build` exits 0
    - No `src/app/api/` directory exists
  </acceptance_criteria>
  <done>A fresh Next.js 16 + Tailwind 4 + shadcn (Radix/neutral) app builds cleanly with the Cal.com embed package and the seven UI primitives installed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply UI-SPEC design tokens, fonts, and the siteConfig identity module</name>
  <read_first>
    - src/app/globals.css (the CLI-generated :root/.dark blocks to override)
    - src/app/layout.tsx (the CLI-injected Geist font wiring to strip)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Color", "Typography", "Spacing Scale", and the Art Direction anti-pattern gates
    - .planning/phases/01-marketing-foundation/01-RESEARCH.md — Pattern 2 (design tokens), Pattern for fonts, and Pitfall 5 (IBM Plex Sans weight array)
  </read_first>
  <action>
    Override the CLI-generated `:root` block in `src/app/globals.css` with the UI-SPEC palette: `--background: #FAF9F6` (warm paper), `--foreground: #1A2432` (body text on paper), `--primary: #1F6E4A` (deep signal green, CTA-only accent), `--primary-foreground: #FAF9F6`, `--destructive: #B3261E`, and a single restrained `--radius: 0.5rem`. Keep the shadcn `@theme inline` `--color-*` mappings but repoint `--font-sans` to `var(--font-ibm-plex-sans)` and add `--font-heading: var(--font-fraunces)`. Add the near-paper on-dark text color `#F5F3EE` as a supporting token for dark bands. Remove any Nova/Geist-derived font or neon shadow defaults (SITE-06 anti-pattern).

    In `src/app/layout.tsx`, remove the CLI-injected Geist font imports entirely. Load Fraunces via `next/font/google` with `variable: "--font-fraunces"`, `subsets: ["latin"]`, `axes: ["opsz"]`, `display: "swap"` (variable font — no weight array). Load IBM Plex Sans with `weight: ["400", "600"]` (REQUIRED — not variable on Google Fonts; omitting errors the build per Pitfall 5), `variable: "--font-ibm-plex-sans"`, `subsets: ["latin"]`, `display: "swap"`. Apply both `variable` classes to `<html>` or `<body>`. Set the default body font to IBM Plex Sans and provide a `font-heading` utility/class using Fraunces (headings use 600 weight only).

    Create `src/config/site.ts` (D-06) exporting `siteConfig` with exactly the keys in the `<interfaces>` block above, `as const`. Use obvious NEEDS-FOUNDER placeholder strings (e.g. `founderEmail: "PLACEHOLDER_LAUNCH_BLOCKER@example.invalid"`, `calLink: "PLACEHOLDER/free-audit-call"`, `region: "PLACEHOLDER_REGION"`) with an inline comment marking each as a launch blocker and its decision ID. Do NOT invent real biographical or contact values (D-13).

    Add an obviously-temporary founder photo placeholder at `public/founder-placeholder.svg` (a plain labeled gray box that reads "FOUNDER PHOTO — REPLACE BEFORE LAUNCH", per D-07) so the About/home founder blocks have a referenceable asset that is unmistakably not a real headshot.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q '#1F6E4A' src/app/globals.css && grep -q 'IBM_Plex_Sans' src/app/layout.tsx && grep -Eq 'weight:\s*\[\s*"400"\s*,\s*"600"\s*\]' src/app/layout.tsx && ! grep -riq 'geist' src/app/layout.tsx && grep -q 'export const siteConfig' src/config/site.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` `:root` contains `#FAF9F6`, `#1A2432`, `#1F6E4A`, `#B3261E`, and a single `--radius` value
    - `src/app/layout.tsx` imports `Fraunces` and `IBM_Plex_Sans` from `next/font/google`, with IBM Plex Sans declaring `weight: ["400", "600"]`
    - No occurrence of `geist` or `Geist` (case-insensitive) remains in `src/app/layout.tsx`
    - `src/config/site.ts` exports `siteConfig` with keys: name, founderName, founderEmail, region, domain, calLink, calNamespace — every NEEDS-FOUNDER value is an obvious placeholder, not a real value
    - `public/founder-placeholder.svg` exists and visually labels itself as a temporary placeholder
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>The app builds with Fraunces headings + IBM Plex Sans body over the warm-paper/ink-navy/signal-green palette, and every founder-identity value flows from one typed module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package code executes at install/build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | shadcn / create-next-app / @calcom/embed-react installs | mitigate | Install only the RESEARCH Package Legitimacy Audit-approved packages (all "OK", no postinstall scripts); never install `@calcom/react-widget` (nonexistent, 404). No `[ASSUMED]`/`[SUS]` packages in scope, so no blocking human checkpoint required. |
| T-01-02 | Information Disclosure | Phase-scope creep introducing secrets | mitigate | Enforce no `src/app/api/` and no `ANTHROPIC_API_KEY`-shaped env var in Phase 1 (ASVS V14) — verified by the "no api directory" acceptance criterion. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck passed clean in RESEARCH (0 findings across 13 packages); all packages Approved. No blocking checkpoint needed. |
</threat_model>

<verification>
- `npx next build` exits 0 with no font or token errors.
- `components.json`, `globals.css`, `layout.tsx`, and `site.ts` all match their acceptance criteria.
- No `src/app/api/` directory and no secret-shaped env var introduced.
</verification>

<success_criteria>
Scaffold builds cleanly; design tokens and fonts match the UI-SPEC; a single typed `siteConfig` module holds all founder-identity values as flagged placeholders.
</success_criteria>

<output>
Create `.planning/phases/01-marketing-foundation/01-01-SUMMARY.md` when done.
</output>
