---
phase: 01-marketing-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/page.tsx
  - src/components/sections/hero.tsx
  - src/components/sections/demo-placeholder.tsx
  - src/components/sections/outcomes.tsx
  - src/components/sections/engagement-flow.tsx
  - src/components/sections/verticals-teaser.tsx
  - src/components/sections/founder-strip.tsx
  - src/components/sections/final-cta.tsx
autonomous: true
requirements: [SITE-06]
must_haves:
  truths:
    - "The homepage hero headline + demo-placeholder card are the single visual focal point — nothing else competes at Display size or accent weight"
    - "The demo placeholder slot shows the exact UI-SPEC empty-state copy and is shaped to receive the Phase 2 live demo"
    - "The three ROI outcomes render asymmetrically, NOT as a uniform 3-card grid (SITE-06 anti-pattern gate)"
    - "The homepage carries the D-09 section order: hero+demo, outcomes, engagement band, verticals teaser, founder strip, final CTA"
    - "The design reads as deliberately human-designed — no purple gradients, no dark-by-default shell, no emoji/icon-per-feature grid"
    - "D-12: Homepage voice is ROI-first plain language with zero AI jargon in the headline; company-first brand with the founder story as the credibility layer"
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "Display headline + sub-copy + CTA + demo-placeholder slot (the focal point)"
      min_lines: 20
    - path: "src/components/sections/demo-placeholder.tsx"
      provides: "Empty-state card with exact UI-SPEC copy, reserved for Phase 2 demo"
      contains: "The live demo lands here next"
    - path: "src/app/page.tsx"
      provides: "Home route composing all D-09 sections in order"
      min_lines: 15
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*"
      via: "imports and renders each section in D-09 order"
      pattern: "Hero|Outcomes|EngagementFlow|VerticalsTeaser|FounderStrip|FinalCta"
    - from: "src/components/sections/demo-placeholder.tsx"
      to: "Phase 2 live demo slot"
      via: "reserved container matching the UI-SPEC empty state"
      pattern: "The live demo lands here next"
---

<objective>
Build the homepage — the site's design showcase and the SITE-06 gate. The hero headline + demo-placeholder card are THE focal point; below it, the D-09 sections tell the ROI-first story. This is where the "would a design-savvy visitor believe a human made this?" test is won or lost.

Purpose: Satisfy SITE-06 (distinctive, non-templated design) and reserve the Phase 2 demo slot exactly per the UI-SPEC empty state.
Output: A complete, responsive home page composed of reusable section components.
</objective>

## Phase Goal

**As a** skeptical SMB owner, **I want to** browse a credible, custom-designed marketing site and book a free audit call, **so that** I feel confident this consultant can recover real time and money for my business.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-marketing-foundation/01-UI-SPEC.md
@.planning/phases/01-marketing-foundation/01-CONTEXT.md
@.planning/phases/01-marketing-foundation/01-02-SUMMARY.md

<interfaces>
src/config/site.ts exports siteConfig (name, founderName, region, ...)
src/components/book-cta.tsx exports <BookCta /> — the reusable "Book Your Free Audit Call" CTA (reuse; do not re-create the string)
public/founder-placeholder.svg — the temporary founder photo asset (D-07), used by the founder strip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero (focal point) + demo-placeholder empty state + outcomes section</name>
  <read_first>
    - src/components/book-cta.tsx (reuse the CTA)
    - src/components/ui/card.tsx, src/components/ui/skeleton.tsx (demo-placeholder shimmer, optional)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Phase Component Inventory" (hero focal-point note, demo placeholder as Empty state card), "Copywriting Contract" (exact empty-state heading + body), "Typography" (Display clamp), "Color", Art Direction anti-patterns
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-09 (section 1 outcomes asymmetric), D-12 (ROI-first, zero AI jargon headline)
  </read_first>
  <action>
    Create `src/components/sections/hero.tsx`: a Fraunces Display headline using `clamp(32px, 8vw, 44px)` (the only Display instance on the page), ROI-first plain-language sub-copy with zero AI jargon (Claude-drafted per D-12, dollars-and-hours framing), a primary `<BookCta />`, and the demo-placeholder slot beside/below it. The hero headline + placeholder pair is the explicit focal point — everything else on the page must be visually subordinate (no other Display size, no other accent-weight element competing).

    Create `src/components/sections/demo-placeholder.tsx`: an Empty-state card using the VERBATIM UI-SPEC copy — heading "The live demo lands here next" and body "This is where you'll simulate a missed call and watch an instant AI text-back happen live. Coming very soon — want to see it sooner? Book a free audit call and we'll walk through it together." plus a secondary `<BookCta />`. Shape/size this container so the Phase 2 live demo can swap directly into this slot. Optionally use `skeleton` for a subtle loading-shimmer treatment (no neon glow).

    Create `src/components/sections/outcomes.tsx`: the three ROI outcomes (time saved, profit recovered, expenses cut) in an ASYMMETRIC layout — NOT a uniform three-card grid (explicit SITE-06 anti-pattern gate). Use layout weight/scale to differentiate, no emoji or icon-per-feature.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'The live demo lands here next' src/components/sections/demo-placeholder.tsx && grep -q 'clamp(32px, 8vw, 44px)' src/components/sections/hero.tsx && ! grep -riE 'grid-cols-3.*card|linear-gradient.*(purple|violet|#8b5cf6|#a855f7)' src/components/sections/outcomes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/demo-placeholder.tsx` contains the exact heading "The live demo lands here next" and the exact body sentence beginning "This is where you'll simulate a missed call"
    - `src/components/sections/hero.tsx` uses `clamp(32px, 8vw, 44px)` for the Display headline and renders a `<BookCta />`
    - `src/components/sections/outcomes.tsx` does NOT render three equal-width cards in a `grid-cols-3` uniform grid and uses no emoji or purple/violet gradient
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>The hero focal point + verbatim demo-placeholder empty state + asymmetric outcomes section render responsively, reserving the Phase 2 demo slot.</done>
</task>

<task type="auto">
  <name>Task 2: Engagement band + verticals teaser + founder strip + final CTA, composed into the home route</name>
  <read_first>
    - src/config/site.ts (founderName, region)
    - src/components/book-cta.tsx
    - public/founder-placeholder.svg
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-09 (sections 2-5: engagement band on dark ink-navy linking to /services; verticals teaser as plain pain-point copy, teaser only NO links to vertical pages; founder strip photo + one-line FDE story linking to /about; final CTA band), D-12 (company-first voice)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Color" (dark bands #101820, near-paper text), Art Direction gates
  </read_first>
  <action>
    Create `src/components/sections/engagement-flow.tsx`: the compressed audit → project → retainer sequence on a dark ink-navy (#101820) band with near-paper (#F5F3EE) text, rendered as a sequential/numbered flow (NOT a uniform card grid), linking to `/services` for detail.

    Create `src/components/sections/verticals-teaser.tsx`: the four target verticals (home services, medical/dental, legal, real estate) as plain, concrete pain-point copy (text treatment, no icon grid). Teaser ONLY — no links to vertical landing pages (those are Phase 4, D-09).

    Create `src/components/sections/founder-strip.tsx`: the founder credibility strip — the `public/founder-placeholder.svg` photo (D-07 temporary), founder name from `siteConfig`, and a one-line FDE story with clearly-bracketed factual placeholders (no invented biography, D-13), linking to `/about`.

    Create `src/components/sections/final-cta.tsx`: a closing CTA band with a `<BookCta />`.

    Compose `src/app/page.tsx` (Server Component) rendering, in D-09 order: Hero (with demo placeholder), Outcomes, EngagementFlow, VerticalsTeaser, FounderStrip, FinalCta. Set home metadata.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'href=.*/services' src/components/sections/engagement-flow.tsx && grep -q 'href=.*/about' src/components/sections/founder-strip.tsx && ! grep -Eq 'href=.*/(home-services|medical|legal|real-estate|verticals)' src/components/sections/verticals-teaser.tsx && grep -Eq 'Hero' src/app/page.tsx && grep -Eq 'FinalCta' src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/engagement-flow.tsx` renders on a dark #101820 band and links to `/services`
    - `src/components/sections/verticals-teaser.tsx` contains NO links to any vertical/landing-page route (teaser only, D-09)
    - `src/components/sections/founder-strip.tsx` references `public/founder-placeholder.svg`, uses `siteConfig.founderName`, links to `/about`, and uses bracketed placeholders for any biographical fact
    - `src/app/page.tsx` renders the six sections in D-09 order (Hero, Outcomes, EngagementFlow, VerticalsTeaser, FounderStrip, FinalCta)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>The full homepage renders all D-09 sections in order with correct internal links, dark bands, and no premature vertical-page links.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static markup | Home page is fully static SSG; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Static marketing content | accept | No server-side logic, secrets, or input handling in the home page; served static from Netlify edge. No exploitable surface this phase. |
| T-04-02 | Spoofing | Fabricated social proof reintroduced as a "looks legit" fix | mitigate | UI-SPEC + REQUIREMENTS explicitly forbid stock "trusted by" logos and fake ratings — none introduced; verticals teaser uses plain copy only. |
</threat_model>

<verification>
- `npx next build` exits 0; `/` renders all D-09 sections in order.
- Exact empty-state copy present; outcomes asymmetric; no premature vertical links.
- SITE-06 design gate is confirmed at the Plan 06 launch checkpoint (live-site human review).
</verification>

<success_criteria>
A responsive, distinctively-designed homepage with a clear hero focal point, the reserved Phase 2 demo slot, and all D-09 sections — SITE-06 human-designer test confirmed at the Plan 06 launch checkpoint.
</success_criteria>

<output>
Create `.planning/phases/01-marketing-foundation/01-04-SUMMARY.md` when done.
</output>
