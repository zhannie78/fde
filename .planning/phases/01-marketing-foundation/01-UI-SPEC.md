---
phase: 1
slug: marketing-foundation
status: approved
reviewed_at: 2026-07-19
shadcn_initialized: false
preset: "new-york style / neutral base color / CSS variables on (planned — repo not yet scaffolded, see Design System notes)"
created: 2026-07-19
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (planned — no `components.json` or Next.js project exists yet; see note below) |
| Preset | `npx shadcn@latest init -b neutral -s new-york` — neutral base color, New York style, CSS variables enabled. Run this during the Phase 1 scaffolding task, immediately after `create-next-app`, before any page is built. All color tokens below then override the generated neutral palette in `globals.css` / `app/theme` — do not hand-theme individual components. |
| Component library | Radix primitives (via shadcn/ui) |
| Icon library | `lucide-react` — use sparingly, functional only (nav toggle, chevrons, checkmarks). No decorative icon-per-feature grids, no emoji-as-icon anywhere (explicit SITE-06 anti-pattern). |
| Font | **Fraunces** (display/headings, upright — not italic) + **IBM Plex Sans** (body, UI, labels, buttons). Both loaded via `next/font/google`, self-hosted — no external font CDN calls, no FOUT. |

**Why this pairing, not the default:** Inter + a purple gradient + Geist-style everything is the single most-flagged "this looks AI-generated" signal in current design criticism. Fraunces is a characterful, structured serif with real personality that reads as *deliberately chosen*, not a template default — used upright and restrained (not the oversized-italic-serif-on-cream treatment, which has itself become a recognizable "anti-SaaS AI" cliché to avoid). IBM Plex Sans carries an engineering pedigree (IBM) that quietly reinforces the founder's forward-deployed-engineer background without being a jargon word on the page.

### Art Direction & Explicit Anti-Patterns (SITE-06 gate)

This phase's success criterion is literally "would a design-savvy visitor believe a human designer made this." Treat the following as hard *do-not*s, not style suggestions:

- No purple/violet gradients anywhere (the #1 AI-generated-site tell).
- No dark-mode-by-default SaaS look — this audience (HVAC owners, dentists, solo lawyers, real-estate agents) reads dark UI as "tech product for tech people," which undercuts the ROI-first, jargon-free positioning. Site is light-dominant with deliberate dark *bands*, not a dark shell.
- No uniform 3-4 card grid with emoji or generic line icons for the service model — see Services page treatment below; render the audit → project → retainer model as a sequential/numbered flow, not identical feature cards.
- No identical border-radius/shadow/card-height repeated everywhere with a neon-glow drop shadow. One consistent, restrained radius and one soft shadow, used with intent (see Spacing/Typography below).
- No hover states that do nothing and no blanket fade-in-on-scroll for every element. Micro-interactions must be purposeful and specific: link underline draw-on-hover, button background shift + slight lift on hover/focus, nothing more for Phase 1.
- No stock "trusted by" logo strips or fabricated star ratings (already excluded at the requirements level — reinforced here so it isn't accidentally reintroduced as a "looks more legit" fix).
- Founder photo must be a real photo of Annie, not a stock/AI-generated headshot — non-negotiable per SITE-05.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (hero top padding, dark-band section padding) |

Exceptions:
- Mobile nav toggle and sticky mobile CTA bar button: 44px minimum touch target per WCAG 2.5.5 (Target Size, 44×44px), overriding the 4px grid for that one dimension only — a deliberate accessibility citation, not an ad hoc override.
- Sticky mobile CTA bar height: 56px (14 × 4, stays on-grid).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label | 14px | 600 (semibold) | 1.4 — 0.02em letter-spacing, used only for eyebrow text/nav items/form labels, never on headlines |
| Body | 16px | 400 (regular) | 1.6 — slightly more open than the 1.5 default; audience skews non-technical/older, favor legibility |
| Heading | 28px | 600 (semibold) | 1.2 — Fraunces, section headings (H2/H3) |
| Display | 44px | 600 (semibold) | 1.1 — Fraunces, hero headline only. Scale down responsively via `clamp(32px, 8vw, 44px)` on mobile; do not use a second display size elsewhere. |

Exactly 2 weight values used system-wide: 400 (IBM Plex Sans body) and 600 (IBM Plex Sans labels/UI/buttons; Fraunces headings/display). No other weights.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FAF9F6` (warm paper, not stark white, not beige/cream) | Page background on all marketing pages — Home, About, Services |
| Secondary (30%) | `#101820` (ink navy, near-black) | Header/nav bar, footer, alternating dark section bands (e.g. "how the engagement works"), cards rendered on dark bands |
| Accent (10%) | `#1F6E4A` (deep signal green) | Reserved exclusively for: primary CTA button background + its hover/focus state, active nav-link underline, link-hover underline, and highlighted ROI stat numbers (e.g. a callout figure on the Services page). Nothing else uses this color. |
| Destructive | `#B3261E` | Form/embed validation and error-state text/border only (e.g. booking-widget failed-to-load state). No destructive data actions exist in this phase. |

Supporting (not part of the 60/30/10 split, but required for contrast — note for executor):
- Body text on Paper background: `#1A2432` (softened near-navy, not pure black)
- Body text on Ink Navy background: `#F5F3EE` (softened near-paper, not pure white)

Accent reserved for: primary CTA button (all instances — header, hero, sticky mobile bar, footer), active/hover nav-link indicator, link-hover underline, and ROI/highlight numerals only. Never used for decorative shapes, icons, or as a section background.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Book Your Free Audit Call" (every instance of the persistent booking CTA — header, hero, footer, sticky mobile bar) |
| Empty state heading | "The live demo lands here next" (homepage demo placeholder slot — demo embed ships in Phase 2) |
| Empty state body | "This is where you'll simulate a missed call and watch an instant AI text-back happen live. Coming very soon — want to see it sooner? Book a free audit call and we'll walk through it together." + a secondary CTA reusing the Primary CTA button |
| Error state | Booking-widget failed to load: "Couldn't load the booking calendar. Email Annie directly at **[founder email — TBD, pending final domain per PROJECT.md]** and she'll find a time — usually within one business day." |
| Destructive confirmation | None in this phase — no destructive or data-mutating actions exist (no auth, no forms that delete/send). First destructive-adjacent UI is the founder's audit-report review/send screen in Phase 3. |

Note for executor: the founder-email fallback string contains a literal placeholder (`[founder email]`) because the production domain is still marked "Pending" in PROJECT.md's Key Decisions table — resolve this token before shipping the error state, do not ship the bracket text.

---

## Phase Component Inventory

Components this phase actually needs (guides planner task-breakdown and shadcn block selection):

- Site header / nav bar (dark, sticky) with logo, page links, and Primary CTA button
- Mobile nav: `sheet` (slide-out) triggered by a 44px hamburger toggle — icon-only control, MUST carry `aria-label="Open menu"` (and `aria-label="Close menu"` on the dismiss control)
- Sticky mobile CTA bar (bottom-anchored, 56px, Primary CTA only)
- Hero section (Display headline, sub-copy, Primary CTA, and the demo placeholder slot as an `Empty state` card). **Homepage focal point (explicit):** the hero headline + demo placeholder card pair is THE primary visual anchor of the homepage — every other element on the page is visually subordinate to it; nothing else may compete at Display size or with accent-color weight.
- Founder credibility block (About page): real photo, name, region, FDE story copy, `avatar` or custom photo treatment — not a generic bio-card template
- Services model sequence (audit → project → retainer): a numbered/sequential horizontal flow, NOT a 3-card uniform grid — each step differentiated by content weight (step 1 "free" gets the most visual space) rather than by icon
- Footer: real contact info, region, matching-domain email, Primary CTA repeat, page links
- Standard shadcn primitives to pull in: `button`, `card`, `badge`, `navigation-menu`, `sheet`, `separator`, `skeleton` (optional, for the demo-placeholder loading shimmer only)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | button, card, badge, navigation-menu, sheet, separator, skeleton | not required |

No third-party registries declared for this phase. If a third-party block becomes desirable later, the registry vetting gate (view source, scan for `fetch`/`process.env`/`eval`, developer sign-off) must run before it's added to this contract.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
