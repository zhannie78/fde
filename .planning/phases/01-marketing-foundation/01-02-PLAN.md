---
phase: 01-marketing-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/components/site-header.tsx
  - src/components/mobile-nav.tsx
  - src/components/sticky-cta-bar.tsx
  - src/components/site-footer.tsx
  - src/components/book-cta.tsx
autonomous: true
requirements: [SITE-04, SITE-05, SITE-06]
must_haves:
  truths:
    - "Every page shows a persistent 'Book Your Free Audit Call' CTA in the header, and a sticky CTA bar on mobile"
    - "Header nav links to Services and About in that order plus the primary CTA button (D-08)"
    - "The footer shows page links, founder region, and matching-domain contact email sourced from siteConfig"
    - "Mobile visitors can open an accessible slide-out nav via a 44px hamburger with correct aria-labels"
    - "All CTA controls route to /book (no popups in Phase 1, D-02)"
  artifacts:
    - path: "src/components/site-header.tsx"
      provides: "Sticky ink-navy header with nav + primary CTA"
      min_lines: 25
    - path: "src/components/mobile-nav.tsx"
      provides: "Accessible sheet-based mobile nav"
      contains: "aria-label"
    - path: "src/components/site-footer.tsx"
      provides: "Footer with identity + page links + CTA repeat"
      contains: "siteConfig"
    - path: "src/components/sticky-cta-bar.tsx"
      provides: "Bottom-anchored 56px mobile CTA bar"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/site-header.tsx"
      via: "rendered in root layout so it wraps every route"
      pattern: "SiteHeader|site-header"
    - from: "src/components/book-cta.tsx"
      to: "/book"
      via: "Link href to the booking route"
      pattern: "href=.*/book"
---

<objective>
Build the shared layout shell — sticky header with nav + primary CTA, accessible mobile nav, sticky mobile CTA bar, and footer with real identity — and wire it into the root layout so it wraps every current and future route. Add sitemap/robots.

Purpose: SITE-04's "persistent booking CTA on every page" and SITE-05's footer identity are satisfied here once, and the shell becomes the frame that wraps Phase 3's audit funnel and Phase 4's vertical pages.
Output: A reusable layout shell present on all four routes, with a single reusable `<BookCta>` component used everywhere the primary CTA appears.
</objective>

## Phase Goal

**As a** skeptical SMB owner, **I want to** browse a credible, custom-designed marketing site and book a free audit call, **so that** I feel confident this consultant can recover real time and money for my business.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-marketing-foundation/01-UI-SPEC.md
@.planning/phases/01-marketing-foundation/01-CONTEXT.md
@.planning/phases/01-marketing-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Consume the module defined in Plan 01. Do not redefine. -->
src/config/site.ts exports siteConfig with keys:
  name, founderName, founderEmail, region, domain, calLink, calNamespace

<!-- shadcn primitives already installed in Plan 01 (import from @/components/ui/*): -->
  button, navigation-menu, sheet, separator

<!-- BookCta contract this plan creates and later plans reuse: -->
src/components/book-cta.tsx exports:
  <BookCta variant?: "primary" | "sticky"> — renders a Link to /book with the exact copy "Book Your Free Audit Call"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, reusable BookCta, and accessible mobile nav</name>
  <read_first>
    - src/config/site.ts (identity values to render)
    - src/components/ui/sheet.tsx, src/components/ui/button.tsx, src/components/ui/navigation-menu.tsx (installed primitives to compose)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Phase Component Inventory" (header, mobile sheet, 44px toggle, aria-labels), "Color" (ink-navy header, accent CTA), "Copywriting Contract" (exact CTA text)
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-02 (CTAs link to /book, no popups), D-08 (nav order Services, About)
  </read_first>
  <action>
    Create `src/components/book-cta.tsx`: a small reusable component wrapping a Next `Link` to `/book`, rendering the verbatim copy "Book Your Free Audit Call" in the shadcn Button with the `--primary` (signal green #1F6E4A) background. Accept a `variant` prop ("primary" default, "sticky" for the mobile bar). This is the single component every CTA instance uses — do not duplicate the string elsewhere.

    Create `src/components/site-header.tsx`: a sticky, ink-navy (#101820) top bar with the "AI Deployed" wordmark (from `siteConfig.name`) linking to `/`, desktop nav links in order **Services, About** (D-08) using the navigation-menu primitive with an accent (#1F6E4A) active/hover underline, and a `<BookCta />` on the right. Text on the dark bar uses the near-paper #F5F3EE color.

    Create `src/components/mobile-nav.tsx` (Client Component, `"use client"`): a 44px hamburger toggle (WCAG 2.5.5 target size) that opens the shadcn `sheet` slide-out containing the same nav links + a `<BookCta />`. The toggle MUST carry `aria-label="Open menu"` and the dismiss control `aria-label="Close menu"` (UI-SPEC requirement). Use `lucide-react` icons functionally only (menu/close), no decorative icons.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'Book Your Free Audit Call' src/components/book-cta.tsx && grep -q 'aria-label="Open menu"' src/components/mobile-nav.tsx && grep -q 'href=.*/book' src/components/book-cta.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/book-cta.tsx` renders a Link with `href="/book"` and the exact string "Book Your Free Audit Call"
    - `src/components/site-header.tsx` renders nav links to `/services` and `/about` with `/services` appearing before `/about` in source order (D-08)
    - `src/components/mobile-nav.tsx` begins with `"use client"`, opens a `sheet`, and contains both `aria-label="Open menu"` and `aria-label="Close menu"`
    - No hardcoded founder/site strings that duplicate `siteConfig` values (name comes from siteConfig)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>A sticky ink-navy header with correct nav order and a single reusable accent CTA, plus an accessible mobile sheet nav, all routing to /book.</done>
</task>

<task type="auto">
  <name>Task 2: Footer, sticky mobile CTA bar, root layout wiring, sitemap/robots</name>
  <read_first>
    - src/app/layout.tsx (current font wiring from Plan 01 — add shell without removing fonts)
    - src/config/site.ts
    - src/components/book-cta.tsx (created in Task 1)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Spacing Scale" (sticky bar 56px, 44px touch target), "Phase Component Inventory" (footer contents), "Color"
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-08 (footer contents), D-10 (no contact page — footer email covers contact)
  </read_first>
  <action>
    Create `src/components/site-footer.tsx`: an ink-navy (#101820) footer rendering page links (Home, Services, About, Book), the founder `region` and a `mailto:` contact link to `siteConfig.founderEmail` (D-10 — no separate contact page), and a repeated `<BookCta />`. Text uses near-paper #F5F3EE. Every identity value comes from `siteConfig` — no literals.

    Create `src/components/sticky-cta-bar.tsx` (Client Component): a bottom-anchored bar, 56px tall (14×4, on-grid), visible only on mobile (hidden at the desktop breakpoint), containing a single full-width `<BookCta variant="sticky" />` with a 44px minimum touch target. Do not show it on desktop where the header CTA already persists.

    Wire the shell into `src/app/layout.tsx`: render `<SiteHeader />` (with the mobile-nav toggle inside it), then `{children}`, then `<SiteFooter />`, then `<StickyCtaBar />`. Set root `<metadata>` (title template using `siteConfig.name`, description, Open Graph basics) — SEO basics are Claude's discretion per CONTEXT. Add `src/app/sitemap.ts` and `src/app/robots.ts` using Next file conventions covering `/`, `/about`, `/services`, `/book`.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'siteConfig.founderEmail' src/components/site-footer.tsx && grep -Eq '56|h-14' src/components/sticky-cta-bar.tsx && grep -q 'SiteFooter' src/app/layout.tsx && test -f src/app/sitemap.ts && test -f src/app/robots.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/site-footer.tsx` renders a `mailto:` link built from `siteConfig.founderEmail` and shows `siteConfig.region`
    - `src/components/sticky-cta-bar.tsx` is 56px tall (`h-14` or explicit 56px), mobile-only, and uses `<BookCta variant="sticky" />`
    - `src/app/layout.tsx` renders `<SiteHeader />`, `{children}`, `<SiteFooter />`, and `<StickyCtaBar />` in that order and still contains the Plan 01 font wiring
    - `src/app/sitemap.ts` and `src/app/robots.ts` exist and reference all four routes
    - `npx next build` exits 0; `/sitemap.xml` and `/robots.txt` are generated
  </acceptance_criteria>
  <done>Header, footer, and sticky mobile CTA wrap every route via the root layout; sitemap/robots generated; all identity flows from siteConfig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static markup | Marketing pages are static; no untrusted input reaches the app in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Founder email exposed in footer `mailto:` | accept | Publishing a business contact email is the intended behavior for a lead-gen site; no PII beyond a public business address. Value sourced from siteConfig, resolved to real address only at launch. |
| T-02-02 | Spoofing | Client Components (`mobile-nav`, `sticky-cta-bar`) | accept | No secrets or auth in these components; they are pure UI state — no trust boundary crossed. |
| T-02-SC | Tampering | shadcn `add` for any additional primitive | mitigate | Use only official-registry primitives already vetted in Plan 01; no third-party registries (UI-SPEC Registry Safety). |
</threat_model>

<verification>
- `npx next build` exits 0; header/footer/sticky CTA render on every route.
- Exact CTA copy, nav order, aria-labels, and sticky-bar height match acceptance criteria.
- sitemap.xml and robots.txt generated covering all four routes.
</verification>

<success_criteria>
A persistent, accessible layout shell wraps all routes with a single reusable accent CTA linking to /book, footer identity from siteConfig, and generated SEO files.
</success_criteria>

<output>
Create `.planning/phases/01-marketing-foundation/01-02-SUMMARY.md` when done.
</output>
