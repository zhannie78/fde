---
phase: 01-marketing-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/about/page.tsx
  - src/app/services/page.tsx
  - src/components/founder-block.tsx
  - src/components/service-sequence.tsx
autonomous: true
requirements: [SITE-02, SITE-03]
must_haves:
  truths:
    - "A visitor can read an About page telling the founder's FDE story as the company credibility layer"
    - "A visitor can read a Services page explaining the white-glove model plainly: free audit → fixed-scope project → monthly retainer"
    - "The service model renders as a sequential/numbered flow where step 1 (free) has the most visual weight — NOT a uniform 3-card grid"
    - "All biographical specifics are clearly-bracketed placeholders — no invented founder facts"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About route with founder FDE story"
      min_lines: 15
    - path: "src/app/services/page.tsx"
      provides: "Services route with the engagement model"
      min_lines: 15
    - path: "src/components/service-sequence.tsx"
      provides: "The audit → project → retainer sequential flow (not a card grid)"
      min_lines: 20
    - path: "src/components/founder-block.tsx"
      provides: "Founder credibility block (real-photo treatment, not a bio-card template)"
      contains: "founder-placeholder"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/founder-block.tsx"
      via: "renders the founder credibility block"
      pattern: "FounderBlock|founder-block"
    - from: "src/app/services/page.tsx"
      to: "src/components/service-sequence.tsx"
      via: "renders the engagement-model sequence"
      pattern: "ServiceSequence|service-sequence"
---

<objective>
Build the two credibility content pages: About (SITE-02 — the founder's FDE story as the human trust layer) and Services (SITE-03 — the white-glove engagement model rendered as a deliberate sequential flow, not a templated card grid).

Purpose: Establish the credibility and engagement-model layers a skeptical SMB owner needs before booking. These are static content pages that reuse the layout shell and design system.
Output: Working `/about` and `/services` routes with reusable founder-block and service-sequence components.
</objective>

## Phase Goal

**As a** skeptical SMB owner, **I want to** browse a credible, custom-designed marketing site and book a free audit call, **so that** I feel confident this consultant can recover real time and money for my business.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-marketing-foundation/01-UI-SPEC.md
@.planning/phases/01-marketing-foundation/01-CONTEXT.md
@.planning/phases/01-marketing-foundation/01-02-SUMMARY.md

<interfaces>
src/config/site.ts exports siteConfig (name, founderName, region, ...)
src/components/book-cta.tsx exports <BookCta />
public/founder-placeholder.svg — temporary founder photo asset (D-07)
src/components/ui/* — badge, card, separator available if needed (use restrained, not a uniform grid)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: About page + founder credibility block (SITE-02)</name>
  <read_first>
    - src/config/site.ts (founderName, region)
    - public/founder-placeholder.svg
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Phase Component Inventory" (Founder credibility block: real photo, name, region, FDE story; NOT a generic bio-card template), "Typography", "Color"
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-12 (company-first: "AI Deployed, and here's the person who deploys it", not a personal blog), D-13 (bracketed biographical placeholders, no invented facts)
  </read_first>
  <action>
    Create `src/components/founder-block.tsx`: the founder credibility block — the `public/founder-placeholder.svg` photo with a deliberate custom treatment (NOT a generic centered avatar bio-card), `siteConfig.founderName`, `siteConfig.region`, and the FDE story. Draft the FDE narrative structure and tone per D-12 (company-first credibility layer — "I embedded with enterprise clients; now I embed AI in your business") but put every factual specific (real name-as-presented, work-history beats, specific companies/years) in clearly-bracketed placeholders like `[FDE work-history beat — NEEDS-FOUNDER]` (D-13). Never invent a biography.

    Create `src/app/about/page.tsx` (Server Component): a Fraunces heading, the `<FounderBlock />`, supporting company-first narrative copy, and a `<BookCta />` closing. Set About metadata.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'founder-placeholder' src/components/founder-block.tsx && grep -q 'siteConfig' src/components/founder-block.tsx && grep -Eq 'FounderBlock' src/app/about/page.tsx && grep -Eq 'NEEDS-FOUNDER|\[.*\]' src/components/founder-block.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/founder-block.tsx` references `public/founder-placeholder.svg`, uses `siteConfig.founderName` and `siteConfig.region`, and contains at least one clearly-bracketed `NEEDS-FOUNDER` biographical placeholder
    - The founder block is not a generic centered avatar+name+role bio-card (deliberate custom treatment per UI-SPEC)
    - `src/app/about/page.tsx` renders `<FounderBlock />` and a `<BookCta />`
    - `/about` appears in the build route list; `npx next build` exits 0
  </acceptance_criteria>
  <done>The About page tells the company-first FDE credibility story with a custom founder-photo treatment and no invented facts.</done>
</task>

<task type="auto">
  <name>Task 2: Services page + audit → project → retainer sequential flow (SITE-03)</name>
  <read_first>
    - src/components/book-cta.tsx
    - .planning/phases/01-marketing-foundation/01-RESEARCH.md — Pattern 3 (numbered/sequential flow, NOT a card grid; step 1 gets the most visual weight)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — Art Direction anti-pattern (no uniform 3-4 card grid for the service model), "Color" (accent reserved for ROI/highlight numerals)
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-03/service model (free audit → fixed-scope project → monthly retainer), D-12 (plain language)
  </read_first>
  <action>
    Create `src/components/service-sequence.tsx`: the engagement model as a sequential/numbered flow (RESEARCH Pattern 3) — three steps (1. Free Audit, 2. Fixed-Scope Project, 3. Monthly Retainer) rendered so step 1 ("free") gets the most visual space/copy, differentiated by content weight and layout, NOT by icons and NOT as three identical uniform cards (explicit SITE-06 anti-pattern gate). Use an asymmetric column structure (e.g. a wider first column). Accent color (#1F6E4A) may be used only on a highlighted ROI/step numeral, per UI-SPEC.

    Create `src/app/services/page.tsx` (Server Component): a Fraunces heading, plain-language intro copy (Claude-drafted per D-11/D-12, no AI jargon), the `<ServiceSequence />`, and a closing `<BookCta />`. Set Services metadata.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -Eq 'ServiceSequence' src/app/services/page.tsx && ! grep -Eq 'grid-cols-3' src/components/service-sequence.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/service-sequence.tsx` renders the three named steps in order (Free Audit → Fixed-Scope Project → Monthly Retainer) as an asymmetric sequential flow, NOT a `grid-cols-3` uniform grid, and uses no per-step icons/emoji
    - Step 1 (Free Audit) has visibly more space/copy than steps 2 and 3
    - `src/app/services/page.tsx` renders `<ServiceSequence />` and a `<BookCta />`
    - `/services` appears in the build route list; `npx next build` exits 0
  </acceptance_criteria>
  <done>The Services page explains the white-glove model as a deliberate numbered flow with step 1 emphasized, not a templated card grid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static markup | Both pages are fully static SSG; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Static About/Services content | accept | No server logic, secrets, or input handling; served static from Netlify edge. No exploitable surface this phase. |
| T-05-02 | Repudiation | Invented founder biography facts undermining trust | mitigate | D-13 enforced — all biographical specifics are bracketed NEEDS-FOUNDER placeholders resolved only by the founder at launch; no fabricated facts ship. |
</threat_model>

<verification>
- `npx next build` exits 0; `/about` and `/services` render.
- Founder block uses bracketed placeholders; service sequence is asymmetric (not a card grid).
</verification>

<success_criteria>
Working About and Services pages establishing the credibility and engagement-model layers, both using the design system and reusing the CTA, with no invented founder facts.
</success_criteria>

<output>
Create `.planning/phases/01-marketing-foundation/01-05-SUMMARY.md` when done.
</output>
