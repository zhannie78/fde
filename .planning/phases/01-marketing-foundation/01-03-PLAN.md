---
phase: 01-marketing-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/book/page.tsx
  - src/components/cal-embed.tsx
autonomous: true
requirements: [SITE-04]
must_haves:
  truths:
    - "A visitor at /book sees a real Cal.com scheduling embed loaded from Cal.com directly (no app server hop)"
    - "If the embed fails to load, the visitor sees the UI-SPEC error state with a founder-email fallback"
    - "The Cal embed is isolated to a small client component; the rest of /book stays server-rendered"
    - "The embed uses siteConfig.calLink and namespace — no hardcoded scheduling link"
  artifacts:
    - path: "src/components/cal-embed.tsx"
      provides: "Client Component wrapping @calcom/embed-react with error-state handling"
      contains: "@calcom/embed-react"
    - path: "src/app/book/page.tsx"
      provides: "The /book route with surrounding copy + the embed"
      min_lines: 15
  key_links:
    - from: "src/components/cal-embed.tsx"
      to: "siteConfig.calLink"
      via: "calLink prop passed to <Cal>"
      pattern: "siteConfig\\.calLink"
    - from: "src/components/cal-embed.tsx"
      to: "siteConfig.founderEmail"
      via: "mailto fallback in the error state"
      pattern: "founderEmail"
---

<objective>
Build the `/book` route — the single place the entire site's persistent CTA points to — with an inline Cal.com embed and the UI-SPEC failed-to-load error state. This is the Walking Skeleton's one real third-party interaction that proves the full client stack.

Purpose: Satisfy SITE-04 (book a call via a persistent scheduling CTA) with D-02's single-embed-page approach and D-03's error state.
Output: A working `/book` page; after this plan, the local full-stack path (home → CTA → /book → Cal embed) is exercisable end to end.
</objective>

## Phase Goal

**As a** skeptical SMB owner, **I want to** browse a credible, custom-designed marketing site and book a free audit call, **so that** I feel confident this consultant can recover real time and money for my business.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-marketing-foundation/01-UI-SPEC.md
@.planning/phases/01-marketing-foundation/01-RESEARCH.md
@.planning/phases/01-marketing-foundation/01-CONTEXT.md
@.planning/phases/01-marketing-foundation/01-02-SUMMARY.md

<interfaces>
src/config/site.ts exports siteConfig with keys:
  founderName, founderEmail, calLink, calNamespace (= "free-audit-call")

<!-- @calcom/embed-react@1.5.3 API (from RESEARCH Pattern 1 — verified package, thin official docs): -->
  import Cal, { getCalApi } from "@calcom/embed-react"
  getCalApi({ namespace }) -> cal("ui", { theme, layout, styles: { branding: { brandColor } } })
  <Cal namespace calLink style config={{ layout }} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cal.com embed client component with error-state fallback</name>
  <read_first>
    - src/config/site.ts (calLink, calNamespace, founderName, founderEmail)
    - .planning/phases/01-marketing-foundation/01-RESEARCH.md — Pattern 1 (the exact getCalApi/Cal usage), Pitfall 2 (no @calcom/react-widget), Pitfall 3 (empty-div ref bug → use v1.5.3), Pitfall 4 (theme flash — set theme in ONE place)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Copywriting Contract" Error state, "Color" (destructive #B3261E)
  </read_first>
  <action>
    Create `src/components/cal-embed.tsx` as a Client Component (`"use client"`) wrapping `@calcom/embed-react`. In a `useEffect`, call `getCalApi({ namespace: siteConfig.calNamespace })` and configure UI in exactly ONE place via `cal("ui", { theme: "light", hideEventTypeDetails: false, layout: "month_view", styles: { branding: { brandColor: "#1F6E4A" } } })` — do NOT also set theme on the `<Cal>` config prop (Pitfall 4 theme flash). Render `<Cal namespace={siteConfig.calNamespace} calLink={siteConfig.calLink} config={{ layout: "month_view" }} style={{ width: "100%", height: "100%", minHeight: "700px" }} />`.

    Wrap the `getCalApi` call in try/catch; on failure set a `failed` state and render the UI-SPEC error state: a `role="alert"` block with `#B3261E` destructive text/border reading "Couldn't load the booking calendar. Email [founderName] directly at [founderEmail] and she'll find a time — usually within one business day." — with `founderName` and `founderEmail` interpolated from `siteConfig` (resolve the UI-SPEC's literal `[founder email]` placeholder token via the config, never ship the bracket text). The email is a `mailto:` link.

    Never import or reference `@calcom/react-widget` (does not exist).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && head -1 src/components/cal-embed.tsx | grep -q 'use client' && grep -q '@calcom/embed-react' src/components/cal-embed.tsx && grep -q 'siteConfig.calLink' src/components/cal-embed.tsx && grep -q 'role="alert"' src/components/cal-embed.tsx && ! grep -q 'react-widget' src/components/cal-embed.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/cal-embed.tsx` starts with `"use client"` and imports `Cal, { getCalApi }` from `@calcom/embed-react`
    - The `<Cal>` `calLink` is `siteConfig.calLink` (not a literal string)
    - Theme is set in exactly one place (the `cal("ui", ...)` call), not also on the `<Cal>` config prop
    - The error state is a `role="alert"` element interpolating `siteConfig.founderName` and a `mailto:` to `siteConfig.founderEmail`; no literal `[founder email]` bracket text remains
    - No reference to `@calcom/react-widget`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>An isolated client component renders the Cal.com embed with a single-source theme and a founder-email error fallback, all values from siteConfig.</done>
</task>

<task type="auto">
  <name>Task 2: /book route composing surrounding copy + the embed</name>
  <read_first>
    - src/components/cal-embed.tsx (the client component from Task 1)
    - src/app/layout.tsx (the shell that wraps this route)
    - .planning/phases/01-marketing-foundation/01-CONTEXT.md — D-02 (single /book embed page, no modals), D-12 (ROI-first plain voice)
    - .planning/phases/01-marketing-foundation/01-UI-SPEC.md — "Typography" (Fraunces heading), "Color"
  </read_first>
  <action>
    Create `src/app/book/page.tsx` as a Server Component (keep the `"use client"` boundary limited to `<CalEmbed />` only). Render a Fraunces H1/Display heading and short ROI-framed sub-copy (Claude-drafted per D-11/D-12 — plain language, dollars-and-hours framing, no AI jargon; this is Claude's discretion within UI-SPEC limits) above the embed, then render `<CalEmbed />` in a container sized to give the embed room (min-height ~700px). Set page metadata (title/description) for the route. Do not add popup/modal booking — inline only (D-02).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && grep -q 'CalEmbed' src/app/book/page.tsx && ! grep -q 'use client' src/app/book/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/book/page.tsx` renders `<CalEmbed />` and does NOT itself declare `"use client"` (server component; only the embed is client)
    - Page renders a Fraunces heading + ROI-framed sub-copy above the embed
    - Route `/book` is present in the build output route list
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Visiting /book renders surrounding copy plus the live Cal embed; the local end-to-end skeleton path (home CTA → /book → embed) works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → Cal.com (3rd party) | The embed loads a script/iframe from cal.com's origin directly in the visitor's browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Compromised/malicious Cal.com embed script | mitigate | Load the embed ONLY on the dedicated `/book` page (D-02), never site-wide — limits blast radius if the vendor script ever misbehaves (RESEARCH Security Domain). Cal.com is a trusted vendor; no app secret is exposed (public scheduling link only). |
| T-03-02 | Denial of Service | Embed fails to load, dead-ends the visitor | mitigate | UI-SPEC error state with founder-email fallback (D-03) ensures a failed embed still yields a booking path. |
| T-03-03 | Information Disclosure | API key leakage | accept/N-A | No secret involved — Cal.com embed uses only a public `username/event-type` link, not an API key. No server proxy needed this phase. |
</threat_model>

<verification>
- `npx next build` exits 0; `/book` appears in the route list.
- Cal embed uses siteConfig.calLink; error state interpolates founder identity; theme set once.
- No `@calcom/react-widget` reference anywhere.
- Note: real end-to-end booking requires the founder's live Cal.com account + calLink (resolved in Plan 06 launch checklist). Buildable/verifiable against the placeholder calLink now.
</verification>

<success_criteria>
The /book page renders a real Cal.com embed with a founder-email error fallback, isolated to a small client boundary, all identity from siteConfig.
</success_criteria>

<output>
Create `.planning/phases/01-marketing-foundation/01-03-SUMMARY.md` when done.
</output>
