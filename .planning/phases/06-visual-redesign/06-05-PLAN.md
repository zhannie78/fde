---
phase: 06-visual-redesign
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - public/annie-photo.jpg
  - src/components/about/avatar.tsx
  - src/app/about/page.tsx
  - src/app/sitemap.ts
autonomous: true
requirements: [DSGN-05, DSGN-01]
user_setup: []
must_haves:
  truths:
    - "A visitor can open /about and see the founder's name, photo, and bio (DSGN-05)"
    - "If the photo file 404s, a monogram placeholder renders instead of a broken image"
    - "Credentials appear as pills; portfolio shows 4 glow-box composite case cards with the illustrative-composite disclosure"
    - "The About page final CTA uses the same ElevatedCta treatment as the homepage"
    - "/about appears in the sitemap"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About/bio/portfolio Server Component route with metadata"
      contains: "ElevatedCta"
      min_lines: 40
    - path: "src/components/about/avatar.tsx"
      provides: "Client Component next/image with onError monogram fallback"
      exports: ["Avatar"]
    - path: "public/annie-photo.jpg"
      provides: "Founder photo asset served from public/"
    - path: "src/app/sitemap.ts"
      provides: "/about sitemap entry"
      contains: "/about"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/about/avatar.tsx"
      via: "import Avatar"
      pattern: "Avatar"
    - from: "src/components/about/avatar.tsx"
      to: "/annie-photo.jpg"
      via: "next/image src + onError fallback"
      pattern: "annie-photo"
---

<objective>
Build the new `/about` page that makes the anonymity reversal (DSGN-05) concrete: founder photo (with graceful monogram fallback), name and bio, credential pills, an honest demos section, a 4-card composite portfolio with the required disclosure line, and the same elevated final CTA as the homepage — all on the Phase 6 visual system. Move the photo asset into `public/` and register the route in the sitemap.

Purpose: DSGN-05 (founder name/photo/bio on a new /about page) + DSGN-01 (consistent visual system).
Output: `/about` route + `Avatar` component + relocated photo + sitemap entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md
@.claude/skills/sketch-findings-fde/references/bio-portfolio-page.md
@.claude/skills/sketch-findings-fde/sources/002-about-page/index.html

<interfaces>
<!-- Consumes primitives/CSS from plan 06-01. -->
From src/components/ui/glow-box.tsx:     export function GlowBox({ className?, children }): JSX
From src/components/ui/elevated-cta.tsx:  export function ElevatedCta({ className? }): JSX
From src/config/site.ts:                  siteConfig.founderName  // "Annie" — import; do not hardcode
globals.css (plan 06-01) provides: .avatar-wrap, .avatar-photo, .avatar-placeholder, .credential-pill, .demo-badge, .case-result

<!-- src/app/page.tsx is the analog Server-Component route (metadata export + section composition). -->
<!-- annie-photo.jpg currently sits at REPO ROOT — must move to public/ or next/image 404s (RESEARCH Pitfall 5). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move photo to public/ and build the Avatar fallback component</name>
  <files>public/annie-photo.jpg, src/components/about/avatar.tsx</files>
  <read_first>
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (avatar.tsx entry — exact onError pattern)
    - .planning/phases/06-visual-redesign/06-RESEARCH.md (Pattern 4 + Pitfall 5 — next/image onError, public/ requirement)
    - .claude/skills/sketch-findings-fde/references/bio-portfolio-page.md (Avatar CSS + placeholder monogram)
    - src/components/cal-embed.tsx (codebase precedent for a small useState-failure-flag Client Component)
  </read_first>
  <action>
    Run `git mv annie-photo.jpg public/annie-photo.jpg` (RESEARCH Pitfall 5 — files outside `public/` are not servable and would 404, masking as a "handled" fallback). Create `src/components/about/avatar.tsx` as a `"use client"` component (a function prop `onError` forces the client boundary — keep it small, just the avatar). Export `Avatar`. Use `useState(false)` for a `failed` flag. When `failed`, render the monogram placeholder: a `.avatar-wrap` containing a `.avatar-placeholder` (or inline styled div) showing the first initial of `siteConfig.founderName` on the accent-soft gradient background. Otherwise render `.avatar-wrap` containing a Next `Image` (`import Image from "next/image"`) with `src="/annie-photo.jpg"`, `alt={siteConfig.founderName}`, explicit `width={200}` `height={200}` (CLS protection, DSGN-04), className `avatar-photo rounded-full object-cover`, and `onError={() => setFailed(true)}`. Import `siteConfig` from `@/config/site` — derive the monogram initial from `siteConfig.founderName`, do not hardcode "A". Do NOT use a raw `<img onerror>` string (RESEARCH: use the React `onError` state pattern).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && test -f public/annie-photo.jpg && ! test -f annie-photo.jpg && grep -q '"use client"' src/components/about/avatar.tsx && grep -q "onError" src/components/about/avatar.tsx && grep -q 'width={200}' src/components/about/avatar.tsx && grep -q "siteConfig.founderName" src/components/about/avatar.tsx && ! grep -q "onerror" src/components/about/avatar.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `public/annie-photo.jpg` exists and repo-root `annie-photo.jpg` no longer exists
    - `Avatar` is a `"use client"` component using `next/image` with explicit 200x200 dimensions and an `onError` monogram fallback derived from `siteConfig.founderName`
    - No raw `<img onerror=...>` string; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Photo served from public/; Avatar renders the real photo with a React monogram fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /about page (bio header, demos, portfolio, final CTA)</name>
  <files>src/app/about/page.tsx</files>
  <read_first>
    - src/app/page.tsx (analog route — metadata export + Server Component composition)
    - .claude/skills/sketch-findings-fde/references/bio-portfolio-page.md (full page structure, content, disclosure line, avoid-list)
    - .claude/skills/sketch-findings-fde/sources/002-about-page/index.html (validated bio prose + portfolio composite content — draw copy from here, do not invent new biographical claims)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Phase Component Inventory: About section; Copywriting Contract portfolio disclosure)
    - src/components/ui/glow-box.tsx, src/components/ui/elevated-cta.tsx (primitives)
  </read_first>
  <action>
    Create `src/app/about/page.tsx` as a Server Component (default export `About`) with its own `export const metadata: Metadata` (title/description using buyer vocabulary + founder framing, matching `page.tsx`'s convention). No custom header/footer needed — the root layout already wraps every route. Compose, in order: (1) Bio header — a 2-column desktop grid (`~220px 1fr`), single-column centered on mobile: left column renders `<Avatar />` (import from `@/components/about/avatar`); right column renders the name (`Hello, I'm {siteConfig.founderName}` as a Heading-role h1, NOT Display — Display is Hero-only), a title/subtitle, the credential pills, and a bio prose paragraph. Credential pills: two `.credential-pill` badges reading exactly "M.S., Georgia Tech — 4.0 GPA" and "Palantir Foundry Certified Application Developer" (confirmed content). Do NOT add industry-vertical pills (explicitly reverted — verticals go in the bio prose only). (2) Demos section — a 2-col card grid: one card badged "Live" (`.demo-badge`) linking to the real ROI calculator at `/#calculator`; any additional card badged "Preview" with a "Request a walkthrough" CTA — NEVER fabricate a fake "Live" demo. (3) Portfolio section — 2-col grid of exactly 4 `GlowBox` case cards, each with a number label (01–04), a title, and three labeled blocks (The Problem / The Solution / The Result); wrap the Result block in `.case-result` (lime left-border). Draw the composite case content from the sketch source file. Render the required disclosure line VISIBLY (not buried) near the portfolio heading: "Representative examples of the kind of systems I build — illustrative composites based on the patterns I deploy most often, not client-identifying case studies." (4) Final CTA — render `<ElevatedCta />` (identical treatment to the homepage FinalCta — must match exactly, per the skill file). Import `siteConfig` and use `siteConfig.founderName` for the name; never hardcode "Annie". Use weights 400/700 only.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && test -f src/app/about/page.tsx && grep -q "export const metadata" src/app/about/page.tsx && grep -q "Avatar" src/app/about/page.tsx && grep -q "ElevatedCta" src/app/about/page.tsx && grep -q "credential-pill" src/app/about/page.tsx && grep -q "Georgia Tech" src/app/about/page.tsx && grep -q "Palantir Foundry" src/app/about/page.tsx && grep -q "illustrative composites" src/app/about/page.tsx && grep -q "/#calculator" src/app/about/page.tsx && grep -q "siteConfig.founderName" src/app/about/page.tsx && [ "$(grep -c 'GlowBox' src/app/about/page.tsx)" -ge "4" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `/about` route exists as a Server Component with a `metadata` export
    - Renders `Avatar`, two `.credential-pill` badges with the exact confirmed credential strings, and NO industry-vertical pills
    - Demos section has a "Live" card linking to `/#calculator` and any extra card is "Preview" (no fake Live)
    - Portfolio has ≥4 `GlowBox` case cards and the exact illustrative-composite disclosure line is present
    - Final CTA is `ElevatedCta`; name derives from `siteConfig.founderName`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/about renders the full bio/portfolio page on the new system with honest demos and disclosed composites.</done>
</task>

<task type="auto">
  <name>Task 3: Add /about to the sitemap</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (file being modified — routes array)
  </read_first>
  <action>
    Add `"/about"` to the `routes` array so it becomes `["", "/book", "/about"]`. No other change — the existing `.map()` builds the entry automatically.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q '"/about"' src/app/sitemap.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `routes` array includes `"/about"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/about registered in the sitemap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static /about content → shipped HTML | About page is static server-rendered content — NO forms, NO user input surface (per phase brief). |
| photo asset load (browser) | `next/image` loads a first-party `public/` asset; onError fallback is the only client behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | founder bio/portfolio content | mitigate | Portfolio cards carry the mandatory illustrative-composite disclosure line — prevents implying false/real client claims; credentials limited to the two sketch-confirmed items. |
| T-06-10 | Spoofing | "Live" vs "Preview" demo cards | mitigate | Only the real ROI calculator (`/#calculator`) is badged "Live"; all other demos are "Preview" — no fabricated live demo can mislead a visitor. |
| T-06-11 | Denial of Service | missing photo asset | mitigate | `onError` monogram fallback guarantees a rendered avatar even if `annie-photo.jpg` fails to load — no broken-image state. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; grep gates pass.
- Manual (optional): `npm run dev`, open `/about`, confirm the real photo renders (not the monogram), credential pills show, 4 portfolio cards with disclosure, and the elevated CTA matches the homepage.
</verification>

<success_criteria>
- /about reachable, renders founder name/photo/bio (DSGN-05) on the new system.
- Honest demos, disclosed composite portfolio, matching elevated CTA.
- Photo served from public/; route in sitemap.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-05-SUMMARY.md` when done.
</output>
