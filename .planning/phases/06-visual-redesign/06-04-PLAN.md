---
phase: 06-visual-redesign
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/book-cta.tsx
  - src/components/site-header.tsx
  - src/components/mobile-nav.tsx
  - src/components/site-footer.tsx
  - src/components/sticky-cta-bar.tsx
autonomous: true
requirements: [DSGN-01, DSGN-05]
must_haves:
  truths:
    - "Every CTA across the site reads 'Book a Free Call Now' (single-source change via book-cta.tsx)"
    - "Desktop nav and mobile nav both expose an 'About' link to /about (DSGN-05 reachability)"
    - "Header, footer, mobile nav, and sticky bar render in the new mono/indigo/paper system"
  artifacts:
    - path: "src/components/book-cta.tsx"
      provides: "Single CTA component with updated label"
      contains: "Book a Free Call Now"
    - path: "src/components/site-header.tsx"
      provides: "Desktop nav with About link"
      contains: "/about"
    - path: "src/components/mobile-nav.tsx"
      provides: "Mobile nav with About link"
      contains: "/about"
  key_links:
    - from: "src/components/site-header.tsx"
      to: "/about"
      via: "navLinks entry"
      pattern: "/about"
    - from: "src/components/book-cta.tsx"
      to: "all CTA call sites"
      via: "single shared component"
      pattern: "Book a Free Call Now"
---

<objective>
Restyle the persistent site chrome (header, mobile nav, footer, sticky CTA bar) to the Phase 6 system, rename the CTA label everywhere via the single `BookCta` component, and add the "About" navigation link that makes the new `/about` route reachable (DSGN-05).

Purpose: DSGN-01 (chrome consistency with the redesign) + DSGN-05 (About page reachable from global nav).
Output: Five updated chrome/CTA components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md

<interfaces>
<!-- Current CTA label + nav arrays (extracted so no codebase spelunking is needed). -->
book-cta.tsx: both branches render `<Link href="/book">Book Your Free Audit Call</Link>` — change BOTH occurrences to read from `siteConfig.bookCtaLabel` (added by plan 06-01 in `src/config/site.ts`, value `"Book a Free Call Now"`) instead of a hardcoded string literal, so `book-cta.tsx` and `elevated-cta.tsx` (06-01) both source the label from the same constant and can never drift. Every call site (header, hero, offer, final-cta was, sticky bar, footer, mobile nav, About) renders through this one component (D-02) — do not hardcode the string anywhere else.
site-header.tsx navLinks: `[{ href: "/#offer", label: "Pricing" }, { href: "/#process", label: "How It Works" }]`
mobile-nav.tsx navLinks: same two entries (duplicated array — follow existing duplication convention).
Chrome components reference only theme utilities (`bg-secondary`, `text-secondary-foreground`, `text-primary`, `bg-background`) — palette flows automatically from plan 06-01; the work here is the CTA label, the About link, and the font-weight sweep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CTA label in book-cta.tsx</name>
  <files>src/components/book-cta.tsx</files>
  <read_first>
    - src/components/book-cta.tsx (file being modified — both glow and plain branches)
    - src/config/site.ts (plan 06-01 adds `siteConfig.bookCtaLabel = "Book a Free Call Now"` here — import and use it, do not reintroduce a hardcoded literal)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Copywriting Contract — Primary CTA)
  </read_first>
  <action>
    Import `siteConfig` from `@/config/site` if not already imported. Change the link text in BOTH branches (the `glow` branch and the plain `Button` branch) from the literal `Book Your Free Audit Call` to `{siteConfig.bookCtaLabel}`. Do not change the `href="/book"`, the props, or the component structure. This single edit propagates the new label to every call site (D-02 discipline) and keeps `book-cta.tsx` and `elevated-cta.tsx` reading from the same constant.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && [ "$(grep -c 'siteConfig.bookCtaLabel' src/components/book-cta.tsx)" = "2" ] && ! grep -q "Book Your Free Audit Call" src/components/book-cta.tsx && ! grep -q '"Book a Free Call Now"' src/components/book-cta.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "siteConfig.bookCtaLabel" src/components/book-cta.tsx` returns `2`
    - Zero occurrences of the literal "Book Your Free Audit Call" or a hardcoded "Book a Free Call Now" string remain in the file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both CTA branches read the new label from the shared `siteConfig.bookCtaLabel` constant; propagates site-wide.</done>
</task>

<task type="auto">
  <name>Task 2: Add About nav link and restyle header + mobile nav</name>
  <files>src/components/site-header.tsx, src/components/mobile-nav.tsx</files>
  <read_first>
    - src/components/site-header.tsx (file being modified — navLinks array + wordmark)
    - src/components/mobile-nav.tsx (file being modified — duplicated navLinks array)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Component Inventory chrome row — About link)
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (site-header/mobile-nav entry)
  </read_first>
  <action>
    In BOTH `site-header.tsx` and `mobile-nav.tsx`, add `{ href: "/about", label: "About" }` to the `navLinks` array (follow the existing duplication convention — do not introduce a shared constants file). Chrome palette flows automatically from plan 06-01 (`bg-secondary` ink band, `text-primary` accent hovers) — no hex edits. Fix any `font-semibold`/`font-medium` in these two files to `font-bold`/`font-normal` (e.g. site-header wordmark `font-semibold`, mobile-nav link `font-medium`) to honor the exactly-2-weights contract. Keep the wordmark, nav structure, `BookCta`, and mobile 44px touch targets unchanged. The About link may appear before or after the two existing links — place it last for stable ordering.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q '"/about"' src/components/site-header.tsx && grep -q '"/about"' src/components/mobile-nav.tsx && grep -q 'label: "About"' src/components/site-header.tsx && grep -q 'label: "About"' src/components/mobile-nav.tsx && [ "$(grep -c 'font-semibold\|font-medium' src/components/site-header.tsx src/components/mobile-nav.tsx | grep -v ':0' | wc -l | tr -d ' ')" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both files' `navLinks` include `{ href: "/about", label: "About" }`
    - Zero `font-semibold`/`font-medium` remain in either file
    - Wordmark, `BookCta`, and 44px mobile targets preserved
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>About link present in desktop + mobile nav; chrome weights corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Weight/style sweep for footer + sticky CTA bar</name>
  <files>src/components/site-footer.tsx, src/components/sticky-cta-bar.tsx</files>
  <read_first>
    - src/components/site-footer.tsx (file being modified — footer links + siteConfig usage)
    - src/components/sticky-cta-bar.tsx (file being modified — sticky mobile CTA)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Component Inventory chrome row)
  </read_first>
  <action>
    These two components reference only theme utilities, so the new palette/radius flows automatically from plan 06-01. The only edits: replace any `font-semibold`/`font-medium` with `font-bold`/`font-normal` (e.g. footer wordmark `font-semibold`) to honor the exactly-2-weights contract, and confirm the sticky bar button keeps its 44px (`h-11`) minimum touch target. Do NOT change footer links, `siteConfig` usage, or structure. (Optionally add an "About" footer link to the `footerLinks` array for parity — allowed but not required.) Preserve all `siteConfig`-sourced values (never hardcode founder identity).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && [ "$(grep -c 'font-semibold\|font-medium' src/components/site-footer.tsx src/components/sticky-cta-bar.tsx | grep -v ':0' | wc -l | tr -d ' ')" = "0" ] && grep -q "siteConfig" src/components/site-footer.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Zero `font-semibold`/`font-medium` remain in either file
    - Footer still sources identity from `siteConfig` (no hardcoded literals)
    - Sticky bar retains a 44px-min button
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Footer + sticky bar carry the new weights; structure and config usage intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static nav/footer markup → shipped HTML | No user input; nav hrefs are static internal routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Spoofing | footer `mailto` + nav links | accept | `mailto:` and route hrefs derive from `siteConfig` (author-controlled) or static internal paths; no user-supplied URL, no open-redirect surface. |
| T-06-08 | Tampering | shared CTA label | mitigate | Single-source `BookCta` edit keeps the CTA string/destination consistent across all call sites — prevents a drifted/malicious CTA target slipping into one location. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; grep gates pass.
- Manual (optional): `npm run dev`, confirm every CTA reads "Book a Free Call Now" and the About link appears in both desktop and mobile nav.
</verification>

<success_criteria>
- CTA label updated site-wide from one file.
- About link reachable from desktop + mobile nav (DSGN-05).
- Chrome renders in the new system with weights 400/700 only.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-04-SUMMARY.md` when done.
</output>
