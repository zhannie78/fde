---
phase: 06-visual-redesign
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - package.json
  - src/components/scroll-story-provider.tsx
  - src/app/page.tsx
autonomous: false
requirements: [DSGN-02, DSGN-03, DSGN-04]
user_setup: []
must_haves:
  truths:
    - "gsap + @gsap/react are installed only after a human legitimacy check clears them"
    - "Scrolling the landing page plays the 5-act scroll-driven story via ScrollTrigger"
    - "A visitor with prefers-reduced-motion:reduce gets ZERO ScrollTrigger instances (fully static, readable page)"
    - "GSAP loads only in a client wrapper — the six content sections stay Server Components"
  artifacts:
    - path: "src/components/scroll-story-provider.tsx"
      provides: "'use client' useGSAP + matchMedia wrapper housing all ScrollTrigger choreography"
      exports: ["ScrollStoryProvider"]
    - path: "src/app/page.tsx"
      provides: "Sections wrapped in ScrollStoryProvider (order unchanged)"
      contains: "ScrollStoryProvider"
    - path: "package.json"
      provides: "gsap + @gsap/react dependencies"
      contains: "gsap"
  key_links:
    - from: "src/app/page.tsx"
      to: "ScrollStoryProvider"
      via: "wraps the 7 sections as children"
      pattern: "ScrollStoryProvider"
    - from: "src/components/scroll-story-provider.tsx"
      to: "section selector hooks"
      via: "gsap.matchMedia + ScrollTrigger targeting .hero-lede/[data-thefix-card]/etc"
      pattern: "matchMedia"
---

<objective>
Add the scroll-driven storytelling layer (DSGN-02) with a strict reduced-motion gate (DSGN-03) and CWV-protective bundle discipline (DSGN-04): install GSAP behind a package-legitimacy human check, build the single `"use client"` `ScrollStoryProvider` that houses all ScrollTrigger choreography via `useGSAP` + `gsap.matchMedia()`, and wrap the homepage sections in it without converting any section to a Client Component.

Purpose: DSGN-02 (choreography), DSGN-03 (matchMedia reduced-motion gate), DSGN-04 (client-only GSAP, transform/opacity-only tweens).
Output: Installed GSAP, the provider component, and the wired `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-RESEARCH.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md

<interfaces>
<!-- Provider targets these hooks added by plans 06-02 / 06-03 (selector text, NOT refs). -->
hero:    `.hero-section` (trigger), `.hero-lede` (scrub parallax targets)
the-fix: `.thefix-section`, `[data-thefix-card]` (batch stagger)
outcomes: `.outcomes-section`, `[data-outcome-card]`, `.stat-numeral`, `[data-countup][data-countup-to]` (count-up)
roi-calc: `.roi-calculator-section` (one-shot fade/scale-in)
offer:   `.offer-section`, `[data-offer-card]`, `[data-offer-lead]` (asymmetric stagger)
process: `.process-section`, `[data-process-step]`, `.process-progress-line` (scrubbed fill)
final-cta: `.finalcta-section`, `.finalcta-panel` (one-shot scale-in)

<!-- Packages to install (RESEARCH Package Legitimacy Audit — both [ASSUMED], gate before install): -->
gsap@3.15.0  (github.com/greensock/GSAP, no postinstall scripts)
@gsap/react@2.1.2  (github.com/greensock/react, useGSAP hook, peer react>=17)

<!-- page.tsx is a Server Component composing 7 sections in locked order:
     Hero, TheFix, Outcomes, RoiCalculator, Offer, ProcessTransparency, FinalCta -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for gsap + @gsap/react</name>
  <what-built>Nothing installed yet. This gate verifies the two new npm dependencies (`gsap`, `@gsap/react`) are legitimate before install. RESEARCH.md marked both `[ASSUMED]` because `slopcheck` could not run in the research environment; per the package-legitimacy protocol they require a blocking human check that cannot be auto-approved.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/gsap — confirm it is the official GreenSock package (publisher greensock/Webflow), ~12 years old, ~4M weekly downloads, source repo github.com/greensock/GSAP, no postinstall scripts.
    2. Open https://www.npmjs.com/package/@gsap/react — confirm it is the official GreenSock React integration (source github.com/greensock/react), current 2.1.x, ~1M weekly downloads.
    3. Note (informational, not a blocker): gsap ships under GreenSock's "Standard 'no charge' license" (not OSI/MIT) but permits unrestricted commercial use since the 2025 Webflow change — fine for this billed-client site.
    4. If both check out, approve. If anything looks off (typosquat name, unexpected publisher, install hooks), reject.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `npm install gsap @gsap/react`, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install GSAP and build the ScrollStoryProvider</name>
  <files>package.json, src/components/scroll-story-provider.tsx</files>
  <read_first>
    - .planning/phases/06-visual-redesign/06-RESEARCH.md (Pattern 1 full worked example, Pattern 2 matchMedia, Pitfalls 1/2/4, INP will-change guidance)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Motion & Performance Contract — the 5-act table + reduced-motion + CWV budget)
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (scroll-story-provider.tsx entry — cal-embed analog, small client boundary)
    - src/components/sections/hero.tsx, the-fix.tsx, outcomes.tsx, offer.tsx, process-transparency.tsx, final-cta.tsx, roi-calculator.tsx (to confirm the exact selector hooks landed)
  </read_first>
  <action>
    Only after the checkpoint is approved: run `npm install gsap @gsap/react`. Create `src/components/scroll-story-provider.tsx` as a `"use client"` component exporting `ScrollStoryProvider({ children })`. Static-import at module top: `gsap` from `"gsap"`, `{ ScrollTrigger }` from `"gsap/ScrollTrigger"`, `{ useGSAP }` from `"@gsap/react"` (this is SSR-safe — `useGSAP`'s effect never runs server-side; do NOT use `next/dynamic({ssr:false})`, RESEARCH Pitfall 2). Hold a `containerRef` via `useRef<HTMLDivElement>(null)`. Inside `useGSAP(() => {...}, { scope: containerRef })`: call `gsap.registerPlugin(ScrollTrigger)`, create `const mm = gsap.matchMedia()`, then `mm.add("(prefers-reduced-motion: no-preference)", () => { ... })` containing EVERY effect, and `mm.add("(prefers-reduced-motion: reduce)", () => { /* create ZERO ScrollTriggers — page already ships settled */ })`. Implement the 5 acts against the selector hooks: (Act 1) scrub parallax on `.hero-lede` — opacity 1→0.3, y 0→-24, `scrollTrigger: { trigger: ".hero-section", start: "top top", end: "bottom top", scrub: true }`, no entrance. (Act 2) `ScrollTrigger.batch("[data-thefix-card]", { start: "top 75%", once: true, onEnter: b => gsap.from(b, { opacity: 0, y: 18, stagger: 0.12, duration: 0.6 }) })`. (Act 3) count-up: for each `[data-countup]` element, read `data-countup-to`, tween a proxy 0→target (`duration: 1.2`, `ease: "power2.out"`) writing the integer into the element while preserving any non-numeric suffix, triggered once at `start: "top 70%"`; then a one-shot fade/scale-in on `.roi-calculator-section` (offset ~+0.15s, no scrub). (Act 4) asymmetric stagger: `[data-offer-lead]` enters translateX(-24)→0 + fade first, other `[data-offer-card]` translateY(18)→0 + fade staggered, `start: "top 75%"`, once. (Act 5) ProcessTransparency: scrub-fill `.process-progress-line` (scaleX 0→1 or width) tied to `.process-section` scroll range, `scrub: true`; FinalCta one-shot scale-in `.finalcta-panel` 0.96→1 + fade at `start: "top 80%"`. Use `ScrollTrigger.batch()` for multi-card effects (fewer instances, INP budget). Scope `will-change: transform` to active tweens only via `onStart`/`onComplete` — never leave it globally set (INP). All tweens touch ONLY transform/opacity (CLS). Return `<div ref={containerRef} className="contents">{children}</div>` (if `display:contents` mismeasures triggers during QA, drop `contents` for a plain `<div>` — RESEARCH Pitfall 4). Keep this the ONLY new Client Component; do NOT add `"use client"` to any section (RESEARCH Pitfall 1). Do NOT migrate `glow.tsx` to GSAP.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q '"gsap"' package.json && grep -q '"@gsap/react"' package.json && test -d node_modules/gsap && grep -q '"use client"' src/components/scroll-story-provider.tsx && grep -q "useGSAP" src/components/scroll-story-provider.tsx && grep -q "matchMedia" src/components/scroll-story-provider.tsx && grep -q "prefers-reduced-motion: reduce" src/components/scroll-story-provider.tsx && grep -q "registerPlugin(ScrollTrigger)" src/components/scroll-story-provider.tsx && ! grep -q "ssr: false" src/components/scroll-story-provider.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `gsap` and `@gsap/react`; `node_modules/gsap` exists (install ran post-approval)
    - Provider is `"use client"`, uses `useGSAP` with `{ scope: containerRef }`, and wraps all effects in `gsap.matchMedia()` with BOTH a `no-preference` branch (all effects) and a `reduce` branch (zero ScrollTriggers)
    - All 5 acts target the documented selector hooks; no `ssr: false` anywhere; no section file gained `"use client"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GSAP installed post-gate; provider houses the full choreography with a hard reduced-motion gate and CWV-safe tween discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap homepage sections in ScrollStoryProvider</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (file being modified — Server Component composing the 7 sections in locked order)
    - src/components/scroll-story-provider.tsx (the wrapper built in Task 2)
    - .planning/phases/06-visual-redesign/06-RESEARCH.md (Pattern 1 page.tsx example — Server Components passed as children)
  </read_first>
  <action>
    Add `import { ScrollStoryProvider } from "@/components/scroll-story-provider"`. Wrap the existing 7 sections (`Hero`, `TheFix`, `Outcomes`, `RoiCalculator`, `Offer`, `ProcessTransparency`, `FinalCta`) — in their UNCHANGED locked order — inside `<ScrollStoryProvider>...</ScrollStoryProvider>`, replacing the current `<>...</>` fragment. Keep `page.tsx` a Server Component (no `"use client"`) — the sections remain server-rendered children passed through the client wrapper (RESEARCH Pattern 1). Do NOT reorder, add, or remove sections. Keep the `metadata` export untouched.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "ScrollStoryProvider" src/app/page.tsx && ! grep -q '"use client"' src/app/page.tsx && grep -q "<Hero />" src/app/page.tsx && grep -q "<FinalCta />" src/app/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` wraps all 7 sections in `ScrollStoryProvider`, order unchanged, and stays a Server Component
    - `npm run build` completes successfully (SSR-safe — no ssr:false / no server-side GSAP execution error)
  </acceptance_criteria>
  <done>Homepage sections wrapped; production build passes with the animation layer wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build (supply chain) | New third-party dependencies (gsap, @gsap/react) enter the build. |
| client GSAP layer → DOM | Animation runs post-hydration on server-rendered markup; no user input consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | `npm install gsap @gsap/react` | mitigate | Blocking human-verify checkpoint before install (both packages `[ASSUMED]` per RESEARCH); manual verification against npmjs.com official GreenSock listings; both have no postinstall scripts. Checkpoint is non-auto-approvable. |
| T-06-12 | Denial of Service | ScrollTrigger scroll listener (INP/CWV) | mitigate | `ScrollTrigger.batch()` limits live instances; `will-change` scoped to active tweens only; transform/opacity-only tweens (no layout thrash); GSAP client-only so it never blocks LCP — verified in plan 07. |
| T-06-13 | Denial of Service | prefers-reduced-motion users | mitigate | `matchMedia` `reduce` branch creates ZERO ScrollTriggers; the plan 06-01 blanket CSS override is the JS-independent fallback of last resort. |
</threat_model>

<verification>
- Checkpoint approved before any install.
- `npm run build` passes (SSR-safe wiring).
- grep + tsc gates pass. Full DSGN-03/DSGN-04 confirmation happens in plan 07.
</verification>

<success_criteria>
- GSAP installed only after human legitimacy approval.
- 5-act scroll story wired via a single client wrapper; sections stay Server Components.
- Reduced-motion gate creates zero triggers; production build succeeds.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-06-SUMMARY.md` when done.
</output>
