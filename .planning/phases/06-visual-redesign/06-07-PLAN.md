---
phase: 06-visual-redesign
plan: 07
type: execute
wave: 4
depends_on: [06-04, 06-05, 06-06]
files_modified: [lighthouserc.js, package.json]
autonomous: false
requirements: [DSGN-01, DSGN-03, DSGN-04]
user_setup: []
must_haves:
  truths:
    - "No v1 visual system survives — no Fraunces/IBM Plex, no old palette hexes, no font-semibold/font-medium anywhere in src"
    - "prefers-reduced-motion:reduce yields a fully static, readable page with zero scroll/motion effects"
    - "The built landing page meets mobile Core Web Vitals budgets (LCP <2.5s, CLS <0.1, INP <200ms) with the animation layer active"
  artifacts: []
  key_links: []
---

<objective>
Close the phase by verifying the three acceptance-critical properties the automated per-plan gates cannot fully prove: DSGN-01 (the v1 visual system is fully gone and the new one is applied consistently), DSGN-03 (reduced-motion produces a static readable page), and DSGN-04 (mobile Core Web Vitals hold up with the animation layer). This is a verification-only plan — no source changes.

Purpose: Sign off DSGN-01/03/04 with real evidence, not assumptions.
Output: A production build confirmation, a reduced-motion human check, and a mobile Lighthouse human check recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-RESEARCH.md

<interfaces>
<!-- v1 tokens that MUST be fully absent from src/ after Phase 6 (DSGN-01). -->
Fonts: Fraunces, IBM_Plex_Sans / --font-fraunces / --font-ibm-plex-sans
Weights: font-semibold, font-medium (new system is 400/700 only)
v1 palette hexes: #1F6E4A (green), #101820 (navy), #FAF9F6 (old paper), #1A2432 (old ink), #F5F3EE (old on-dark)
<!-- New system must be present: #3552FF, #FBFAF7, #0C0C0D, JetBrains_Mono -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated DSGN-01 consistency sweep + production build</name>
  <files></files>
  <read_first>
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Color/Typography — confirm the exact target/forbidden tokens)
    - src/app/globals.css (confirm new tokens present, old absent)
  </read_first>
  <action>
    Run a grep sweep across `src/` proving the v1 visual system is gone and the new one is present. Confirm ZERO matches for each forbidden token in `.tsx`/`.css` sources: `Fraunces`, `IBM_Plex_Sans`, `--font-fraunces`, `--font-ibm-plex-sans`, `font-semibold`, `font-medium`, and the v1 hexes `#1F6E4A`, `#101820`, `#1A2432`, `#FAF9F6`, `#F5F3EE`. Confirm the new tokens ARE present in `globals.css`: `#3552FF`, `#FBFAF7`, `#0C0C0D`, and `JetBrains_Mono` in `layout.tsx`. Then run `npm run build` to confirm the entire redesigned site (7 sections + chrome + /about + animation layer) compiles and builds clean. Record the grep results and build outcome in the SUMMARY. If any forbidden token still appears, STOP and report the file/line — do not mark the phase verified.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && ! grep -rniE "Fraunces|IBM_Plex_Sans|--font-fraunces|--font-ibm-plex-sans|font-semibold|font-medium|#1F6E4A|#101820|#1A2432|#FAF9F6|#F5F3EE" src/ && grep -q "#3552FF" src/app/globals.css && grep -q "JetBrains_Mono" src/app/layout.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE "Fraunces|IBM_Plex_Sans|font-semibold|font-medium|#1F6E4A|#101820|#1A2432|#FAF9F6|#F5F3EE" src/` returns zero matches
    - `globals.css` contains `#3552FF`; `layout.tsx` contains `JetBrains_Mono`
    - `npm run build` completes successfully
  </acceptance_criteria>
  <done>v1 system fully removed, new tokens present, full site builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reduced-motion (DSGN-03) human verification</name>
  <what-built>The full Phase 6 redesign with GSAP scroll choreography and the plan 06-01 blanket reduced-motion CSS override + the provider's `matchMedia` `reduce` branch (zero ScrollTriggers).</what-built>
  <how-to-verify>
    1. Enable OS-level reduced motion: macOS System Settings → Accessibility → Display → "Reduce motion" ON (or the equivalent on your OS). Do NOT rely on a code-only review — RESEARCH Pitfall 6 requires the real OS setting.
    2. Run `npm run dev` and open `/` and `/about`.
    3. Scroll the full landing page. CONFIRM: no scroll-linked parallax, no card stagger-in, no count-up, no scrub progress line — every element sits in its final, readable state immediately. No scroll-jacking or pinning.
    4. Hover the glow-box cards and the final CTA. CONFIRM: the blob rotation, shine-sweep, and any ticker/status-pill animation are effectively frozen (durations neutralized by the override).
    5. CONFIRM the page is fully readable and all content is reachable with motion off.
    6. Turn reduced motion OFF, reload, and confirm the 5-act scroll story now plays (parallax, stagger, count-up, scrubbed line, scale-in) — proving the gate toggles both ways.
  </how-to-verify>
  <resume-signal>Type "approved" if the reduced-motion page is fully static/readable and motion returns when the setting is off, or describe what still animates.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add repeatable Lighthouse CI config</name>
  <files>lighthouserc.js, package.json</files>
  <read_first>
    - .planning/phases/06-visual-redesign/06-RESEARCH.md (states the primary recommendation is `@lhci/cli` mobile-throttled runs, not ad hoc DevTools spot checks, so the DSGN-04 gate is repeatable/scriptable by a solo maintainer)
    - package.json (current devDependencies and scripts)
  </read_first>
  <action>
    Add `@lhci/cli` as a devDependency (`npm install --save-dev @lhci/cli`). Create `lighthouserc.js` at the repo root configuring: `ci.collect.staticDistDir` unset (use `ci.collect.startServerCommand: "npm run start"` and `ci.collect.url: ["http://localhost:3000/"]`), `ci.collect.settings.preset: "mobile"` with `throttlingMethod: "simulate"`, and `ci.assert.assertions` for `largest-contentful-paint` (maxNumericValue 2500), `cumulative-layout-shift` (maxNumericValue 0.1), and `total-blocking-time` (maxNumericValue 200, the lab-metric proxy for INP). Add an npm script `"lighthouse": "lhci autorun"` to `package.json`.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && test -f lighthouserc.js && grep -q "largest-contentful-paint" lighthouserc.js && grep -q '"lighthouse": "lhci autorun"' package.json && grep -q "@lhci/cli" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.js` exists at repo root with LCP/CLS/TBT assertions matching the DSGN-04 budgets
    - `package.json` has a `"lighthouse"` script running `lhci autorun` and lists `@lhci/cli` as a devDependency
  </acceptance_criteria>
  <done>A committed, scriptable Lighthouse CI config exists so the DSGN-04 gate is repeatable, not a one-off DevTools spot check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Mobile Core Web Vitals (DSGN-04) human verification</name>
  <what-built>The production build from Task 1, serving the redesigned landing page with the client-only GSAP animation layer, plus the Task 3 `lighthouserc.js` config.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run lighthouse` (this starts the production server per `lighthouserc.js` and runs a mobile-throttled Lighthouse pass against `/` via `@lhci/cli`, replacing an ad hoc DevTools spot check with a repeatable, scriptable run per RESEARCH.md's primary recommendation).
    2. CONFIRM the CLI reports all three assertions (LCP < 2.5s, CLS < 0.1, TBT < 200ms as the INP proxy) as PASS. If `lhci autorun` exits non-zero, an assertion failed.
    3. If any metric is over budget, per RESEARCH the usual cause is image weight or over-broad `will-change` — note which metric failed and by how much so a gap-closure plan can target it (do NOT silently pass). Manual Chrome DevTools Lighthouse is an acceptable one-time fallback only if `@lhci/cli` cannot run in this environment (e.g. no Chrome binary available) — note that explicitly in the SUMMARY if used.
    4. Record the three metric values and the `lhci autorun` exit status in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" with the LCP/CLS/TBT numbers if all three are within budget, or report the failing metric(s) and values.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification + tooling only | Task 3 adds `@lhci/cli` as a devDependency (not shipped to the production bundle or client); Tasks 1/2/4 make no source or dependency changes. No new runtime trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Denial of Service | animation layer vs CWV/accessibility | mitigate | This plan is the verification gate itself — the reduced-motion and mobile-CWV human checks confirm the animation layer does not degrade accessibility (DSGN-03) or performance (DSGN-04) before the phase is signed off. |
| T-06-15 | Tampering | `@lhci/cli` devDependency (Google's official Lighthouse CI tool) | accept | Official `GoogleChrome` org package, widely used, dev-only (never bundled to production/client code) — standard low-risk tooling addition, not a runtime dependency. |
</threat_model>

<verification>
- Task 1 automated sweep + build passes.
- Both human checkpoints approved with recorded evidence (reduced-motion behavior; LCP/CLS/INP numbers).
</verification>

<success_criteria>
- DSGN-01: v1 system fully absent, new system consistent, site builds.
- DSGN-03: reduced-motion yields a static, readable page; motion returns when off.
- DSGN-04: mobile LCP/CLS/INP within budget with the animation layer active.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-07-SUMMARY.md` when done, recording the grep/build result, reduced-motion outcome, and the three CWV numbers.
</output>
