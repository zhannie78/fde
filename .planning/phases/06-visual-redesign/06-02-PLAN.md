---
phase: 06-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/sections/hero.tsx
  - src/components/sections/the-fix.tsx
  - src/components/sections/outcomes.tsx
  - src/components/sections/roi-calculator.tsx
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-05]
must_haves:
  truths:
    - "Hero shows a named 'Hello, I'm Annie —' line linking to /about (anonymity reversal, DSGN-05)"
    - "Hero, TheFix, Outcomes, and RoiCalculator render in the new mono/indigo/paper system with weights 400/700 only"
    - "Each upper section carries stable GSAP selector hooks (className/data-*) distinct from existing nav-anchor ids"
    - "TheFix explainer content uses the glow-box card language"
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "Restyled hero with Display type, Annie/about link, .hero-section/.hero-lede hooks"
      contains: "/about"
    - path: "src/components/sections/the-fix.tsx"
      provides: "Restyled fix section with GlowBox cards + data-thefix-card hooks"
      contains: "data-thefix-card"
    - path: "src/components/sections/outcomes.tsx"
      provides: "Restyled outcomes with stat-numeral count-up hooks + data-outcome-card"
      contains: "stat-numeral"
    - path: "src/components/sections/roi-calculator.tsx"
      provides: "Restyled calculator container (logic unchanged) with .roi-calculator-section hook"
      contains: "roi-calculator-section"
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "/about"
      via: "next/link on founderName"
      pattern: "href=\"/about\""
    - from: "src/components/sections/the-fix.tsx"
      to: "GlowBox"
      via: "import from @/components/ui/glow-box"
      pattern: "GlowBox"
---

<objective>
Restyle the upper half of the landing narrative — Hero (the gap), TheFix (the fix), Outcomes (TIME/EFFICIENCY/PROFIT), and RoiCalculator (proof) — onto the Phase 6 mono/indigo/paper system, add the DSGN-05 "Hello, I'm Annie —" link, and add the stable GSAP selector hooks the scroll-story provider (plan 06) targets. No section reordering, no copy rewrite beyond the explicitly-listed Annie line.

Purpose: DSGN-01 (new visual system on these four sections) + DSGN-02 (selector hooks that make scroll choreography possible) + DSGN-05 (named-founder hero link).
Output: Four restyled section components with GSAP hooks in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md
@.claude/skills/sketch-findings-fde/references/visual-system-and-motion.md

<interfaces>
<!-- Consumes primitives + CSS from plan 06-01. -->
From src/components/ui/glow-box.tsx:  export function GlowBox({ className?, children }): JSX  // renders .glow-box > .glow-box-inner
From src/config/site.ts:  siteConfig.founderName  // "Annie" — import, never hardcode the literal
From globals.css (plan 06-01): typography now Display = clamp(2.75rem,6.4vw,5rem)/lh1/ls-0.035em, Heading = clamp(1.75rem,3.2vw,2.75rem)/lh1.2/ls-0.02em, body lh 1.75, weights 400/700 only.

<!-- GSAP SELECTOR-HOOK CONTRACT (plan 06 targets these by selector text — add exactly these) -->
hero.tsx:   root section className includes `hero-section`; the lede + stat paragraphs each get className `hero-lede`
the-fix.tsx: root section className includes `thefix-section`; each animated card gets attribute `data-thefix-card`
outcomes.tsx: root section className includes `outcomes-section`; each outcome block gets `data-outcome-card`; each stat number span gets className `stat-numeral` and, when the stat begins with a whole integer, `data-countup` + `data-countup-to="<integer>"`
roi-calculator.tsx: root section className includes `roi-calculator-section`
<!-- Do NOT reuse existing anchor ids (#fix,#outcomes,#calculator) as GSAP hooks — those stay for nav scrolling. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Hero + TheFix, add Annie/about link and hooks</name>
  <files>src/components/sections/hero.tsx, src/components/sections/the-fix.tsx</files>
  <read_first>
    - src/components/sections/hero.tsx (file being modified — current structure/copy)
    - src/components/sections/the-fix.tsx (file being modified — current structure/copy)
    - src/config/site.ts (founderName source of truth)
    - src/components/ui/glow-box.tsx (GlowBox props/shape from plan 06-01)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Typography, Component Inventory rows 1-2, Copywriting Contract)
    - .claude/skills/sketch-findings-fde/references/visual-system-and-motion.md (What to Avoid — keep the existing 95%-stat citation; do NOT remove copy)
  </read_first>
  <action>
    Hero: add `import Link from "next/link"` and `import { siteConfig } from "@/config/site"`. Add the root `<section>` className token `hero-section` (keep existing layout utilities). Above the `<h1>`, add an eyebrow line rendering `Hello, I'm ` then a `<Link href="/about" className="...">{siteConfig.founderName}</Link>` then ` —` (DSGN-05 anonymity reversal, per UI-SPEC Component Inventory row 1). Style the Annie link with the accent underline idiom (near-black bold text + `box-shadow: inset 0 -2px 0 0 var(--accent)` or an equivalent accent underline utility) — do NOT flat-recolor it to the accent (UI-SPEC inline-emphasis rule). Change the `<h1>` to Display type: `font-heading font-bold`, inline style `fontSize: "clamp(2.75rem, 6.4vw, 5rem)"`, `lineHeight: 1`, `letterSpacing: "-0.035em"`. Add className `hero-lede` to the two body `<p>` paragraphs and the 95%-stat `<p>` (these are the scrub-parallax targets). Change all `leading-[1.6]` to `leading-[1.75]`, all `font-semibold` to `font-bold`. PRESERVE all existing copy including the "(MIT NANDA, 2025)" citation — Phase 5 LAND-01 requires the stat be sourced; the sketch's citation-removal note is out of scope here.
    TheFix: add `import { GlowBox } from "@/components/ui/glow-box"`. Add root section className token `thefix-section`. Wrap each of the two explainer body paragraphs in a `<GlowBox data-thefix-card>` (glow-box card language per UI-SPEC row 2) so they read as lifted cards; also add `data-thefix-card` to the eyebrow+heading block so it participates in the staggered reveal. Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`. The `text-primary` eyebrow already resolves to indigo via the new token — no color literal needed. Do NOT add an icon grid (established anti-pattern).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q 'href="/about"' src/components/sections/hero.tsx && grep -q "siteConfig.founderName" src/components/sections/hero.tsx && grep -q "hero-lede" src/components/sections/hero.tsx && grep -q "clamp(2.75rem" src/components/sections/hero.tsx && grep -q "data-thefix-card" src/components/sections/the-fix.tsx && grep -q "GlowBox" src/components/sections/the-fix.tsx && [ "$(grep -c 'font-semibold' src/components/sections/hero.tsx src/components/sections/the-fix.tsx | grep -v ':0' | wc -l | tr -d ' ')" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx renders `Hello, I'm <Link href="/about">{siteConfig.founderName}</Link> —` and never hardcodes the string "Annie"
    - hero.tsx h1 uses Display clamp `clamp(2.75rem, 6.4vw, 5rem)`; the two lede paragraphs + stat paragraph carry `hero-lede`
    - the-fix.tsx wraps explainer content in `GlowBox` and every animated card carries `data-thefix-card`
    - Zero `font-semibold` remain in either file (`grep -c font-semibold` = 0 for both)
    - "(MIT NANDA, 2025)" citation text is still present in hero.tsx
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hero + TheFix restyled with new type/palette, Annie link live, glow-box cards and GSAP hooks in place, existing copy preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Outcomes + RoiCalculator, add count-up and container hooks</name>
  <files>src/components/sections/outcomes.tsx, src/components/sections/roi-calculator.tsx</files>
  <read_first>
    - src/components/sections/outcomes.tsx (file being modified — the three-outcome asymmetric layout + stat strings)
    - src/components/sections/roi-calculator.tsx (file being modified — "use client" island, useState logic to PRESERVE)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Component Inventory rows 3-4, Motion contract Act 3, Copywriting Contract empty/result states)
  </read_first>
  <action>
    Outcomes: add root section className token `outcomes-section`. Add `data-outcome-card` to the lead block and to each of the two secondary outcome blocks. Give every stat number span the className `stat-numeral`. For the lead stat that begins with a whole integer ("15+ hrs/week" → integer 15), add `data-countup` and `data-countup-to="15"` on that numeral span so plan 06's provider can tween 0→15 (leave the "+ hrs/week" suffix as static text adjacent to the tweened integer, or keep it inside the span with the provider interpolating only the leading number — implement whichever keeps the suffix visible). For stats with no clean leading integer ("Workflows that run themselves", "$4k–$12k/mo"), add `stat-numeral` only (one-shot fade, no count-up). Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`. Preserve the asymmetric 7/5 grid and the "Even in the worst case, you come out ahead." line (LAND-05).
    RoiCalculator: PRESERVE the `"use client"` directive, `useState` hooks, `WEEKS_PER_YEAR` arithmetic, and both empty/result copy strings EXACTLY (UI-SPEC: "no logic changes"). Add className token `roi-calculator-section` to the root `<section>` (plan 06 does a one-shot fade/scale-in on this container). Restyle the two `<input>` elements and result block to the new system: inputs keep `border border-border bg-card` (these now resolve to #E4E2DB border / #FFFFFF card via plan 06-01) — update `rounded-lg` to match the new radius language if needed, keep 44px (`h-11`) height. Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`. Do NOT add a count-up tween to the dynamic result numerals (they are reactive to input; a per-keystroke count-up would fight React state) — the container fade/scale-in is the only motion here.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "outcomes-section" src/components/sections/outcomes.tsx && grep -q "stat-numeral" src/components/sections/outcomes.tsx && grep -q "data-countup" src/components/sections/outcomes.tsx && grep -q "roi-calculator-section" src/components/sections/roi-calculator.tsx && grep -q '"use client"' src/components/sections/roi-calculator.tsx && grep -q "WEEKS_PER_YEAR = 50" src/components/sections/roi-calculator.tsx && grep -q "leaving on the table" src/components/sections/roi-calculator.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - outcomes.tsx: root carries `outcomes-section`, every stat number span carries `stat-numeral`, lead numeral carries `data-countup data-countup-to="15"`, each outcome block carries `data-outcome-card`
    - outcomes.tsx still contains "Even in the worst case, you come out ahead."
    - roi-calculator.tsx retains `"use client"`, `WEEKS_PER_YEAR = 50`, both empty/result copy strings verbatim, and gains `roi-calculator-section`
    - Zero `font-semibold` remain in either file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Outcomes restyled with count-up hooks; calculator restyled with container hook and logic/copy untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROI calculator inputs (client-only) | Numeric `<input>` values already clamped to `Math.max(0, Number(...))`; no network/persistence — unchanged this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | RoiCalculator numeric inputs | accept | Client-only arithmetic, no API/DB/persistence, existing `Math.max(0, Number(e.target.value) \|\| 0)` clamp is preserved verbatim — no new attack surface added by a pure restyle. |
| T-06-04 | Information Disclosure | Hero `/about` link | accept | Static internal route link; About page (plan 05) exposes only founder-consented public bio content, no secrets. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- grep gates pass.
- Manual (optional): `npm run dev`, load `/`, confirm the four upper sections render in mono/indigo/paper and the Annie link navigates toward `/about` (route lands in plan 05, same wave).
</verification>

<success_criteria>
- Hero, TheFix, Outcomes, RoiCalculator restyled to the new system with weights 400/700 only.
- All GSAP selector hooks present per the contract.
- DSGN-05 Annie/about hero link live; calculator logic + copy unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-02-SUMMARY.md` when done.
</output>
