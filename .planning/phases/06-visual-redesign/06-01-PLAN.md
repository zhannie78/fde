---
phase: 06-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/ui/glow-box.tsx
  - src/components/ui/elevated-cta.tsx
  - src/config/site.ts
autonomous: true
requirements: [DSGN-01, DSGN-03]
must_haves:
  truths:
    - "Every page renders in JetBrains Mono — no Fraunces or IBM Plex Sans loaded"
    - "The indigo #3552FF accent on #FBFAF7 warm-paper background is applied site-wide via CSS custom properties"
    - "A visitor with prefers-reduced-motion:reduce gets a blanket CSS override neutralizing animation/transition durations"
    - "glow-box and elevated-cta primitives exist and are importable by section components"
  artifacts:
    - path: "src/app/globals.css"
      provides: "New 60/30/10 palette, JetBrains Mono font vars, 14px radius, reduced-motion override, reusable glow-box/elevated-cta/avatar/credential/case-result CSS classes"
      contains: "#3552FF"
    - path: "src/app/layout.tsx"
      provides: "JetBrains_Mono next/font import replacing Fraunces + IBM_Plex_Sans"
      contains: "JetBrains_Mono"
    - path: "src/components/ui/glow-box.tsx"
      provides: "Pure-CSS two-layer hover-backlight card wrapper (Server Component)"
      exports: ["GlowBox"]
    - path: "src/components/ui/elevated-cta.tsx"
      provides: "Static dual-glow + shine-sweep final CTA button (Server Component)"
      exports: ["ElevatedCta"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "--font-jetbrains-mono"
      via: "html className interpolating jetBrainsMono.variable"
      pattern: "jetBrainsMono\\.variable"
    - from: "src/components/ui/glow-box.tsx"
      to: ".glow-box CSS in globals.css"
      via: "className string"
      pattern: "glow-box"
---

<objective>
Establish the Phase 6 visual foundation: swap the design tokens (palette, radius, fonts) in `globals.css` and `layout.tsx`, add the blanket reduced-motion CSS override, add every reusable CSS class the sketch findings lock (glow-box, elevated final-CTA, avatar, credential pill, demo badge, case-result), and create the two new pure-CSS React primitives (`GlowBox`, `ElevatedCta`) that the section restyles and About page will consume.

Purpose: This is the load-bearing token/primitive layer for DSGN-01 (new visual system) and the CSS half of DSGN-03 (reduced-motion). Every downstream Wave 2 plan renders correctly only once these tokens and primitives exist.
Output: Updated `globals.css` + `layout.tsx`; new `glow-box.tsx` + `elevated-cta.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md
@.claude/skills/sketch-findings-fde/references/visual-system-and-motion.md
@.claude/skills/sketch-findings-fde/references/bio-portfolio-page.md

<interfaces>
<!-- Current globals.css token block (to REPLACE, not append). Extracted so the executor edits in place. -->
Current @theme inline maps `--font-sans: var(--font-ibm-plex-sans)` and `--font-heading: var(--font-fraunces)`.
Current :root palette (Phase 5): `--background: #FAF9F6`, `--foreground: #1A2432`, `--primary: #1F6E4A`, `--secondary: #101820`, `--radius: 0.5rem`, `--on-dark: #F5F3EE`.
Current @layer base rule: `h1..h6 { @apply font-heading font-semibold; }` (font-semibold = weight 600 — BUG under JetBrains Mono 400/700, see RESEARCH.md Pitfall 3).

Current layout.tsx font wiring:
- imports `Fraunces`, `IBM_Plex_Sans` from `next/font/google`
- `html` className: `${fraunces.variable} ${ibmPlexSans.variable} h-full antialiased`

GlowButton (existing, DO NOT copy its motion approach) lives in `src/components/ui/glow.tsx` — reference only for the `className`/`children` passthrough shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap palette, radius, fonts, base weight, and reduced-motion override in globals.css + layout.tsx</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    - src/app/globals.css (the file being modified — full current token/palette/base blocks)
    - src/app/layout.tsx (the file being modified — current font imports and html className)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Color, Typography, Spacing sections — locked hex values)
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (globals.css + layout.tsx pattern entries with exact before/after)
  </read_first>
  <action>
    In `globals.css` `:root`, replace the Phase 5 palette with the locked 60/30/10 values: `--background: #FBFAF7`, `--foreground: #0C0C0D`, `--card: #FFFFFF`, `--card-foreground: #0C0C0D`, `--popover: #FFFFFF`, `--popover-foreground: #0C0C0D`, `--primary: #3552FF`, `--primary-foreground: #FBFAF7`, `--secondary: #0C0C0D`, `--secondary-foreground: #F4F3EE`, `--muted-foreground: #46484C`, `--accent: #3552FF`, `--accent-foreground: #FBFAF7`, `--border: #E4E2DB`, `--input: #E4E2DB`, `--ring: #3552FF`. Keep `--destructive: #B3261E`. Change `--radius` from `0.5rem` to `0.875rem` (14px card language). Change `--on-dark` to `#F4F3EE`. Add these supporting custom properties in the same `:root` block (plain custom properties, NOT shadcn theme slots per UI-SPEC): `--accent-soft: #EEF1FF`, `--lime: #C8FF4D`, `--pink-glow: #FF6AC1`, `--ink-soft: #46484C`. In `@theme inline`, point BOTH `--font-sans` and `--font-heading` at `var(--font-jetbrains-mono)` (single mono voice — do not add a third font slot). In `@layer base`, change the `h1..h6` rule from `@apply font-heading font-semibold;` to `@apply font-heading font-bold;` and add `letter-spacing: -0.02em;` to that rule; add a `body { line-height: 1.75; }` declaration (UI-SPEC body line-height, up from Phase 5's 1.6). Append the blanket reduced-motion override AFTER all `@layer` blocks (unlayered, so it wins the cascade per RESEARCH.md Pitfall 6): a `@media (prefers-reduced-motion: reduce)` block targeting `*, *::before, *::after` setting `animation-duration: 0.01ms !important`, `animation-iteration-count: 1 !important`, `transition-duration: 0.01ms !important`, `scroll-behavior: auto !important`. In `layout.tsx`, remove the `Fraunces` and `IBM_Plex_Sans` imports and their config objects; add a single `JetBrains_Mono` import from `next/font/google` configured with `variable: "--font-jetbrains-mono"`, `subsets: ["latin"]`, `weight: ["400", "700"]`, `display: "swap"`. Update the `<html>` className to `` `${jetBrainsMono.variable} h-full antialiased` ``. Do NOT touch the `SiteHeader`/`main`/`SiteFooter`/`StickyCtaBar` composition or metadata. Do NOT add `ScrollStoryProvider` here (that wraps sections in `page.tsx`, plan 06).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "#3552FF" src/app/globals.css && grep -q "prefers-reduced-motion" src/app/globals.css && grep -q "JetBrains_Mono" src/app/layout.tsx && ! grep -q "Fraunces\|IBM_Plex_Sans" src/app/layout.tsx && [ "$(grep -c 'font-semibold' src/app/globals.css)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "font-semibold" src/app/globals.css` returns `0` (base heading rule now `font-bold`)
    - `grep "prefers-reduced-motion" src/app/globals.css` matches exactly one media block with `!important` overrides
    - `src/app/layout.tsx` has zero references to `Fraunces` or `IBM_Plex_Sans` and one `JetBrains_Mono({ ... weight: ["400", "700"] ... })` config
    - `:root` contains `--primary: #3552FF`, `--background: #FBFAF7`, `--accent-soft: #EEF1FF`, `--lime: #C8FF4D`, `--radius: 0.875rem`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>New palette/fonts/radius applied, reduced-motion override present, font-weight bug fixed, layout font swapped, types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Append reusable sketch CSS classes to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the file being modified, post-Task-1 state)
    - .claude/skills/sketch-findings-fde/references/visual-system-and-motion.md (glow-box + elevated-CTA CSS, verbatim)
    - .claude/skills/sketch-findings-fde/references/bio-portfolio-page.md (avatar, credential-pill, demo-badge, case-result CSS, verbatim)
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (glow-box.tsx + elevated-cta.tsx entries — the exact CSS excerpts already extracted)
  </read_first>
  <action>
    Append the locked CSS class blocks to the end of `globals.css`, copied VERBATIM from the skill reference files (they are validated — do not re-derive). Add: `.glow-box` / `.glow-box::before` / `.glow-box:hover::before` / `@keyframes glow-box-blob` / `.glow-box-inner` / `.glow-box:hover .glow-box-inner` (the two-layer hover-backlight blob — conic-gradient uses `var(--accent)`, `var(--lime)`, `#ff6ac1`; inner face is `#fff` with `1px solid var(--border)`). Add the elevated final-CTA blocks: `.glow-wrap-final` / `.glow-wrap-final::before` (static conic-gradient glow, `inset: -5px`, no rotation) / `.glow-wrap-final::after` (static radial ambient halo, `inset: -26px`) / `.glow-wrap-final:hover::before` / `.btn-final` / `.btn-final::after` (diagonal shine) / `@keyframes shine-sweep` / `.btn-final:hover` / `.btn-final .arrow` / `.btn-final:hover .arrow`. Add the About-page classes: `.avatar-wrap` (200px circle, `1px solid var(--border)`, drop shadow `0 12px 32px -12px rgba(12,12,13,0.18)`), `.avatar-wrap::after` (inset highlight ring), `.avatar-photo`, `.avatar-placeholder`, `.credential-pill`, `.demo-badge`, `.case-result` / `.case-result p`. Where the skill CSS references `var(--space-N)` tokens or `var(--text-N)` tokens that are not defined in this project's Tailwind v4 setup, substitute concrete pixel values matching the UI-SPEC spacing scale (e.g. `--space-8` → `32px`, `--space-4` → `16px`, `--space-2` → `8px`, `--text-lg` → `1.125rem`, `--text-xs` → `0.75rem`) so the classes are self-contained. Do NOT wrap these in `@layer` (they are component classes referenced by arbitrary-value utilities). Do NOT modify the reduced-motion block from Task 1 — it must remain the last block so its `!important` overrides win over these animations.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && for c in "glow-box-inner" "glow-box-blob" "glow-wrap-final" "btn-final" "shine-sweep" "avatar-wrap" "credential-pill" "demo-badge" "case-result"; do grep -q "$c" src/app/globals.css || { echo "MISSING $c"; exit 1; }; done && grep -q "conic-gradient" src/app/globals.css && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All nine class/keyframe tokens (`glow-box-inner`, `glow-box-blob`, `glow-wrap-final`, `btn-final`, `shine-sweep`, `avatar-wrap`, `credential-pill`, `demo-badge`, `case-result`) are present in `globals.css`
    - `.glow-box::before` and `.glow-wrap-final::before` both use `conic-gradient` with `var(--accent)` and `var(--lime)` stops
    - No `var(--space-` or `var(--text-` reference remains unresolved (all substituted with concrete values)
    - The `@media (prefers-reduced-motion: reduce)` block is the final block in the file
  </acceptance_criteria>
  <done>Every locked sketch CSS pattern present in globals.css with resolved token values; reduced-motion override remains last.</done>
</task>

<task type="auto">
  <name>Task 3: Create GlowBox and ElevatedCta React primitives</name>
  <files>src/components/ui/glow-box.tsx, src/components/ui/elevated-cta.tsx, src/config/site.ts</files>
  <read_first>
    - src/components/ui/glow.tsx (reference ONLY for className/children passthrough shape — do NOT copy its "use client"/motion approach)
    - src/components/book-cta.tsx (CTA destination /book and Link usage convention — Wave 2's 06-04 renames this component's own hardcoded label to the same shared constant added here)
    - src/config/site.ts (single typed source of truth for founder/CTA values, D-06 discipline — add the new CTA label constant here, do not hardcode the string in component files)
    - .planning/phases/06-visual-redesign/06-PATTERNS.md (glow-box.tsx + elevated-cta.tsx pattern entries)
    - src/lib/utils.ts (the `cn` helper import path)
  </read_first>
  <action>
    Add `bookCtaLabel: "Book a Free Call Now"` as a new key on the `siteConfig` object in `src/config/site.ts` (plain string, not a NEEDS-FOUNDER placeholder — this is real, locked copy per the UI-SPEC Copywriting Contract). This is the single source of truth Wave 2's `book-cta.tsx` rename (06-04) will also read from.
    Create `glow-box.tsx` as a plain Server Component (NO `"use client"` — the effect is pure CSS from Task 2). Export `GlowBox`. Props: `className?: string`, `children: React.ReactNode`, and spread the rest onto the outer element. Render an outer `<div className={cn("glow-box", className)}>` containing an inner `<div className="glow-box-inner">{children}</div>` — content MUST be inside `.glow-box-inner`, never directly in `.glow-box` (the glow bleeds -11px and parents must not clip it). Import `cn` from `@/lib/utils`.
    Create `elevated-cta.tsx` as a plain Server Component (NO `"use client"` — static CSS glow + shine). Export `ElevatedCta`. Props: `className?: string`. Render a `<div className="glow-wrap-final">` wrapping a Next `Link` (`import Link from "next/link"`) to `/book` with class `cn("btn-final", className)`, text `{siteConfig.bookCtaLabel}` (import `siteConfig` from `@/config/site` — do NOT hardcode the string literal), and a trailing `<span className="arrow" aria-hidden="true">→</span>`. This is the exact same label as the primary CTA and MUST match the homepage FinalCta and About-page final CTA (UI-SPEC: "the two must match exactly") — routing both through `siteConfig.bookCtaLabel` is what guarantees that match instead of relying on two hand-synced literals. Do NOT introduce GSAP or motion here — the dual-glow and shine are pure CSS.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q 'bookCtaLabel: "Book a Free Call Now"' src/config/site.ts && grep -q "export function GlowBox" src/components/ui/glow-box.tsx && grep -q "glow-box-inner" src/components/ui/glow-box.tsx && ! grep -q '"use client"' src/components/ui/glow-box.tsx && grep -q "export function ElevatedCta" src/components/ui/elevated-cta.tsx && grep -q "siteConfig.bookCtaLabel" src/components/ui/elevated-cta.tsx && ! grep -q '"use client"' src/components/ui/elevated-cta.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/site.ts` exports `siteConfig.bookCtaLabel === "Book a Free Call Now"`
    - `GlowBox` renders nested `.glow-box` > `.glow-box-inner` and is NOT a client component
    - `ElevatedCta` renders `.glow-wrap-final` > Link.btn-final to `/book`, reads its label from `siteConfig.bookCtaLabel` (no hardcoded CTA string literal in the file), and is NOT a client component
    - Both component files pass `npx tsc --noEmit`
  </acceptance_criteria>
  <done>Both primitives compile, are Server Components, reference the Task 2 CSS classes, and source their CTA label from the single shared `siteConfig.bookCtaLabel` constant rather than a hardcoded literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS/font tokens → shipped HTML | No untrusted input; tokens are static and author-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `next/font/google` JetBrains Mono fetch at build | accept | First-party Next.js font pipeline self-hosts the font at build; no runtime third-party font CDN request ships. No new npm dependency added in this plan. |
| T-06-02 | Denial of Service | reduced-motion CSS override | mitigate | Blanket `prefers-reduced-motion` override ships as static, cacheable CSS with zero JS dependency — guarantees a readable static page even if all JS fails (defense for DSGN-03). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `grep` gates in each task's `<automated>` block pass.
- Manual (optional): run `npm run dev`, load `/`, confirm the page renders in monospace on warm-paper background with indigo CTAs (full section restyle lands in Wave 2 — foundation only needs to not break the existing layout).
</verification>

<success_criteria>
- globals.css carries the full new palette, 14px radius, JetBrains Mono font vars, reduced-motion override, and every reusable sketch CSS class.
- layout.tsx loads only JetBrains Mono.
- GlowBox and ElevatedCta exist as Server Components ready for Wave 2 consumers.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-01-SUMMARY.md` when done.
</output>
