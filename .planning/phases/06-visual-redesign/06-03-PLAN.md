---
phase: 06-visual-redesign
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/sections/offer.tsx
  - src/components/sections/process-transparency.tsx
  - src/components/sections/final-cta.tsx
autonomous: true
requirements: [DSGN-01, DSGN-02]
must_haves:
  truths:
    - "Offer pricing steps render as glow-box cards with asymmetric-stagger GSAP hooks"
    - "ProcessTransparency stays a dark full-bleed band on the new #0C0C0D ink, with a scrub-fillable progress line and lime on-dark labels"
    - "FinalCta renders on a light accent-soft inset panel using the ElevatedCta button (not the dark band)"
    - "All three lower sections carry stable GSAP selector hooks distinct from nav-anchor ids"
  artifacts:
    - path: "src/components/sections/offer.tsx"
      provides: "Glow-box pricing cards with data-offer-card / data-offer-lead hooks"
      contains: "data-offer-card"
    - path: "src/components/sections/process-transparency.tsx"
      provides: "Dark band with .process-progress-line and data-process-step hooks, lime on-dark labels"
      contains: "process-progress-line"
    - path: "src/components/sections/final-cta.tsx"
      provides: "accent-soft inset panel + ElevatedCta + .finalcta-panel scale-in hook"
      contains: "ElevatedCta"
  key_links:
    - from: "src/components/sections/final-cta.tsx"
      to: "ElevatedCta"
      via: "import from @/components/ui/elevated-cta"
      pattern: "ElevatedCta"
    - from: "src/components/sections/offer.tsx"
      to: "GlowBox"
      via: "import from @/components/ui/glow-box"
      pattern: "GlowBox"
---

<objective>
Restyle the lower half of the landing narrative — Offer (pricing), ProcessTransparency (how it works), FinalCta (book the audit) — onto the Phase 6 system: glow-box pricing cards, the dark ink band updated with a scrubbable progress line, and the elevated light-panel final CTA. Add the GSAP selector hooks plan 06 targets.

Purpose: DSGN-01 (new visual system) + DSGN-02 (choreography hooks for Acts 4-5).
Output: Three restyled section components with hooks in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visual-redesign/06-UI-SPEC.md
@.planning/phases/06-visual-redesign/06-PATTERNS.md
@.claude/skills/sketch-findings-fde/references/visual-system-and-motion.md

<interfaces>
<!-- Consumes primitives + CSS from plan 06-01. -->
From src/components/ui/glow-box.tsx:    export function GlowBox({ className?, children }): JSX
From src/components/ui/elevated-cta.tsx: export function ElevatedCta({ className? }): JSX  // renders .glow-wrap-final > Link.btn-final "Book a Free Call Now" + arrow

<!-- GSAP SELECTOR-HOOK CONTRACT (plan 06 targets these; do NOT reuse ids #offer/#process/#cta) -->
offer.tsx:   root className `offer-section`; each pricing `<li>` gets `data-offer-card`; the first (widest, index 0) card ALSO gets `data-offer-lead`
process-transparency.tsx: root/inner className `process-section`; each step `<li>` gets `data-process-step`; add ONE new empty `<div className="process-progress-line" aria-hidden="true" />` element the provider scrub-fills
final-cta.tsx: root className `finalcta-section`; the inner panel div gets className `finalcta-panel`

<!-- Color tokens (resolved in plan 06-01): --secondary #0C0C0D, --secondary-foreground #F4F3EE, --accent-soft #EEF1FF, --lime #C8FF4D, --accent #3552FF -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Offer as glow-box asymmetric pricing cards</name>
  <files>src/components/sections/offer.tsx</files>
  <read_first>
    - src/components/sections/offer.tsx (file being modified — 3-step asymmetric ol, scope-qualified prices)
    - src/components/ui/glow-box.tsx (GlowBox from plan 06-01)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Component Inventory row 5, Motion contract Act 4)
  </read_first>
  <action>
    Add `import { GlowBox } from "@/components/ui/glow-box"`. Add root section className token `offer-section`. Wrap each pricing `<li>`'s content in a `<GlowBox>` so the three steps read as the locked card language; keep the existing asymmetric `md:grid-cols-[1.6fr_1fr_1fr]` layout (step 1 widest — SITE-06 anti-uniform-grid gate). Add `data-offer-card` to every card and additionally `data-offer-lead` to the first (index 0) card so plan 06 can give it the translateX(-24px) lead-in while the two narrower cards stagger translateY. The step-1 accent price already resolves to indigo via `text-primary`. Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`. Preserve all price/scope copy verbatim (LAND-02 scope-qualified pricing) and the mid-page `<BookCta />` (LAND-03). Ensure the section wrapper does NOT gain `overflow: hidden` (would clip glow bleed).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "offer-section" src/components/sections/offer.tsx && grep -q "data-offer-card" src/components/sections/offer.tsx && grep -q "data-offer-lead" src/components/sections/offer.tsx && grep -q "GlowBox" src/components/sections/offer.tsx && grep -q "Under \$10k" src/components/sections/offer.tsx && [ "$(grep -c 'font-semibold' src/components/sections/offer.tsx)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - offer.tsx wraps each `<li>` in `GlowBox`, roots at `offer-section`, tags all cards `data-offer-card` and the lead card `data-offer-lead`
    - Scope-qualified price copy ("Under $10k", "Under $2k/mo") preserved; mid-page `BookCta` still rendered
    - Zero `font-semibold` remain; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Offer restyled to asymmetric glow-box cards with stagger hooks; pricing copy and CTA intact.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ProcessTransparency dark band + add scrub progress line</name>
  <files>src/components/sections/process-transparency.tsx</files>
  <read_first>
    - src/components/sections/process-transparency.tsx (file being modified — dark band, numbered steps)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Component Inventory row 6, Color section on-dark lime label rule, Motion Act 5 scrub line)
  </read_first>
  <action>
    Keep the section as a dark full-bleed band. The `bg-secondary`/`text-secondary-foreground` classes now resolve to ink `#0C0C0D` / `#F4F3EE` automatically via plan 06-01 — no hex literal needed. Add the className token `process-section` to the inner container. Add `data-process-step` to each step `<li>`. Add ONE new element `<div className="process-progress-line" aria-hidden="true" />` positioned along the step sequence (e.g. a thin full-width rule the provider scrub-fills via scaleX or width tween tied to section scroll) — the provider owns the animation; this task only adds the target element and gives it a base style (e.g. a 2px line in `var(--border)` with a `var(--accent)` fill child, or a single element the provider scales). The step-number spans currently use `text-primary` (indigo) — on the near-black band indigo has low contrast, so switch the on-dark step-number label color to `var(--lime)` per UI-SPEC (indigo→lime swap ONLY on this dark band). Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`. Preserve all step copy (LAND-04).
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "process-section" src/components/sections/process-transparency.tsx && grep -q "process-progress-line" src/components/sections/process-transparency.tsx && grep -q "data-process-step" src/components/sections/process-transparency.tsx && grep -q "var(--lime)\|lime" src/components/sections/process-transparency.tsx && grep -q "bg-secondary" src/components/sections/process-transparency.tsx && [ "$(grep -c 'font-semibold' src/components/sections/process-transparency.tsx)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Section still uses `bg-secondary`/`text-secondary-foreground` (dark band preserved)
    - Root carries `process-section`; each step `<li>` carries `data-process-step`; one `process-progress-line` element exists
    - Step-number labels use the lime accent on the dark band (not indigo)
    - "How the engagement works" heading + all step copy preserved; zero `font-semibold`; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dark band restyled with lime on-dark labels, scrub-line target element, and step hooks; copy intact.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle FinalCta as elevated light-panel CTA</name>
  <files>src/components/sections/final-cta.tsx</files>
  <read_first>
    - src/components/sections/final-cta.tsx (file being modified — currently dark bg-secondary panel + BookCta glow)
    - src/components/ui/elevated-cta.tsx (ElevatedCta from plan 06-01)
    - .planning/phases/06-visual-redesign/06-UI-SPEC.md (Color FinalCta background exception, Component Inventory row 7, Motion Act 5)
    - .claude/skills/sketch-findings-fde/references/bio-portfolio-page.md ("the two must match exactly" — this treatment is reused on /about)
  </read_first>
  <action>
    Replace the current dark inner panel: change the inner `<div>` from `bg-secondary ... text-secondary-foreground` to a LIGHT tinted panel `bg-[var(--accent-soft)]` with `text-foreground` (near-black ink) and a 20px border-radius (override `rounded-xl` with an explicit `rounded-[20px]` or inline `borderRadius: "20px"`) — UI-SPEC FinalCta background exception (light panel, NOT the dark band). Add root section className token `finalcta-section` and add className `finalcta-panel` to the inner panel div (plan 06 does a one-shot scale-in 0.96→1 on it). Replace the `<BookCta glow />` with `<ElevatedCta />` (the static dual-glow + shine-sweep button — this MUST be the same treatment reused on the About page). Add `import { ElevatedCta } from "@/components/ui/elevated-cta"` and remove the now-unused `BookCta` import if nothing else uses it. Ensure the section wrapper has NO `overflow: hidden` (would clip the ElevatedCta's -26px ambient halo — sketch-confirmed bug). Update the heading/body text color for the light panel (near-black ink), keep the copy verbatim. Change `font-semibold`→`font-bold`, `leading-[1.6]`→`leading-[1.75]`.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q "ElevatedCta" src/components/sections/final-cta.tsx && grep -q "finalcta-panel" src/components/sections/final-cta.tsx && grep -q "accent-soft" src/components/sections/final-cta.tsx && grep -q "Ready to close the gap" src/components/sections/final-cta.tsx && ! grep -q "overflow-hidden" src/components/sections/final-cta.tsx && [ "$(grep -c 'font-semibold' src/components/sections/final-cta.tsx)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Inner panel uses `bg-[var(--accent-soft)]` + `text-foreground` + 20px radius (no `bg-secondary`)
    - `ElevatedCta` replaces `BookCta glow`; root carries `finalcta-section`, panel carries `finalcta-panel`
    - No `overflow-hidden` on the section; "Ready to close the gap?" copy preserved
    - Zero `font-semibold`; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>FinalCta restyled to light accent-soft panel with the elevated CTA and scale-in hook; copy intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static section markup → shipped HTML | No user input in these sections; all content author-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Offer/FinalCta CTA links | accept | CTAs route to the internal `/book` page via the shared `ElevatedCta`/`BookCta` components; no dynamic/user-controlled URL — no injection surface. |
| T-06-06 | Repudiation | pricing copy integrity | accept | Scope-qualified pricing is static server-rendered text preserved verbatim; a pure restyle cannot alter the commercial claims. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; grep gates pass.
- Manual (optional): `npm run dev`, load `/`, confirm offer cards lift on hover, process band is dark with lime numbers, final CTA sits on a light panel with the glowing elevated button (motion wiring lands in plan 06).
</verification>

<success_criteria>
- Offer, ProcessTransparency, FinalCta restyled to the new system with weights 400/700 only.
- All lower-section GSAP hooks present per contract; scrub progress-line element exists.
- FinalCta uses the elevated light-panel treatment; all section copy preserved.
</success_criteria>

<output>
Create `.planning/phases/06-visual-redesign/06-03-SUMMARY.md` when done.
</output>
