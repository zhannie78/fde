---
phase: 05-fde-landing-page
plan: 06
type: execute
wave: 3
depends_on: [05-05]
files_modified:
  - src/app/about/page.tsx
  - src/app/services/page.tsx
  - src/components/founder-block.tsx
  - src/components/service-sequence.tsx
  - src/app/sitemap.ts
  - src/components/site-header.tsx
  - src/components/site-footer.tsx
  - src/components/mobile-nav.tsx
  - src/components/cal-embed.tsx
  - src/app/book/page.tsx
autonomous: true
requirements: [LAND-06]
must_haves:
  truths:
    - "/about and /services routes no longer exist and are absent from the sitemap"
    - "Header, footer, and mobile nav point to in-page anchors (/#offer, /#process) and /book, not deleted routes"
    - "No founder name or gendered pronoun appears in any rendered visitor-facing copy (Cal.com error fallback and /book intro anonymized)"
    - "A full-repo grep for retired v1 vocabulary and for founder-name/pronoun references returns zero rendered-output hits"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "routes array trimmed to home and book only"
      contains: "\"/book\""
    - path: "src/components/cal-embed.tsx"
      provides: "Anonymized booking-failure fallback copy"
      contains: "Email us directly"
  key_links:
    - from: "src/components/site-header.tsx"
      to: "in-page anchors"
      via: "navLinks -> /#offer, /#process"
      pattern: "/#offer"
---

<objective>
Close out LAND-06: delete the /about and /services routes and their identity/sequence components, trim the sitemap, repoint all nav links to in-page anchors, and scrub the last founder-name/pronoun references (Cal.com error fallback, /book intro). Runs after plan 05 so the two exit greps can be asserted across the fully-migrated repo.

Purpose: Ensure zero surviving v1 positioning and zero anonymity violations anywhere in shipped copy, routes, and metadata.
Output: Deleted about/services routes + founder-block + service-sequence; edited sitemap, header, footer, mobile-nav, cal-embed, book page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete /about and /services routes and their orphaned components; trim sitemap</name>
  <files>src/app/about/page.tsx, src/app/services/page.tsx, src/components/founder-block.tsx, src/components/service-sequence.tsx, src/app/sitemap.ts</files>
  <read_first>
    - src/app/about/page.tsx (imports founder-block — deleted together)
    - src/app/services/page.tsx (imports service-sequence — deleted together; content already folded into Offer/ProcessTransparency in wave 1)
    - src/app/sitemap.ts (routes array to trim)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (sitemap edit: routes = ["", "/book"]; Deleted table)
  </read_first>
  <action>
    Delete `src/app/about/page.tsx` and `src/app/services/page.tsx` (single-page IA — no standalone About/Services routes in v2). Delete `src/components/founder-block.tsx` (About-page founder bio, violates anonymity) and `src/components/service-sequence.tsx` (its pattern/content is folded into `Offer` and `ProcessTransparency`; now orphaned). Confirm no remaining imports of `founder-block` or `service-sequence` under `src` before deleting. Edit `src/app/sitemap.ts` so the `routes` array is exactly `["", "/book"]` (was `["", "/about", "/services", "/book"]`); leave the rest of the `sitemap()` function unchanged.
  </action>
  <verify>
    <automated>! test -f src/app/about/page.tsx && ! test -f src/app/services/page.tsx && ! test -f src/components/founder-block.tsx && ! test -f src/components/service-sequence.tsx && ! grep -rn "founder-block\|service-sequence" src --include="*.tsx" --include="*.ts" && ! grep -q "/about\|/services" src/app/sitemap.ts && npm run build 2>&1 | tail -5 && echo ROUTES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `about/page.tsx`, `services/page.tsx`, `founder-block.tsx`, `service-sequence.tsx` no longer exist.
    - No remaining import references to `founder-block` or `service-sequence` under `src`.
    - `sitemap.ts` `routes` array is exactly `["", "/book"]` (no `/about`, no `/services`).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>About/Services routes and their orphaned components are deleted and the sitemap references only home and /book; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint nav links to in-page anchors (header, footer, mobile-nav)</name>
  <files>src/components/site-header.tsx, src/components/site-footer.tsx, src/components/mobile-nav.tsx</files>
  <read_first>
    - src/components/site-header.tsx (navLinks array — currently /services, /about)
    - src/components/site-footer.tsx (footerLinks array — currently includes /services, /about)
    - src/components/mobile-nav.tsx (navLinks array — same /services, /about duplication)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (exact before/after arrays; use leading-slash anchors /#offer, /#process so they resolve from /book)
  </read_first>
  <action>
    Edit only the link arrays (JSX structure unchanged). In `site-header.tsx`, set `navLinks` to `[{ href: "/#offer", label: "Pricing" }, { href: "/#process", label: "How It Works" }]`. In `mobile-nav.tsx`, apply the identical `navLinks` replacement. In `site-footer.tsx`, set `footerLinks` to `[{ href: "/", label: "Home" }, { href: "/#offer", label: "Pricing" }, { href: "/#process", label: "How It Works" }, { href: "/book", label: "Book" }]`. Use leading-slash anchors (`/#offer`, `/#process`) so the links resolve when clicked from `/book`, matching the anchors set on the Offer (`#offer`) and ProcessTransparency (`#process`) sections. Do not touch anything else in these files.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && ! grep -q '"/services"\|"/about"' src/components/site-header.tsx src/components/site-footer.tsx src/components/mobile-nav.tsx && grep -q "/#offer" src/components/site-header.tsx && grep -q "/#offer" src/components/mobile-nav.tsx && grep -q "/#offer" src/components/site-footer.tsx && echo NAV_OK</automated>
  </verify>
  <acceptance_criteria>
    - None of the three nav files reference `/services` or `/about`.
    - Header, footer, and mobile-nav all contain `/#offer` and `/#process` anchor links.
    - Footer still contains `/` (Home) and `/book`.
    - `npm run build` succeeds; JSX structure otherwise unchanged.
  </acceptance_criteria>
  <done>Header, footer, and mobile nav point at in-page anchors and /book with no links to deleted routes.</done>
</task>

<task type="auto">
  <name>Task 3: Anonymity scrub (Cal.com fallback + /book intro) and final LAND-06 grep gate</name>
  <files>src/components/cal-embed.tsx, src/app/book/page.tsx</files>
  <read_first>
    - src/components/cal-embed.tsx (role="alert" fallback string interpolating founderName + "she")
    - src/app/book/page.tsx (metadata.description interpolating founderName)
    - src/config/site.ts (founderName/founderEmail constants — constant stays, only consuming copy changes)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (Copywriting Contract Error state row; Anonymity scrub checklist — the two exit greps)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (exact before/after for cal-embed fallback and book metadata)
  </read_first>
  <action>
    Rewrite the `cal-embed.tsx` `failed` (`role="alert"`) fallback to remove the founder name and gendered pronoun: "Couldn't load the booking calendar. Email us directly at {siteConfig.founderEmail} and we'll find a time — usually within one business day." Keep the `mailto:{siteConfig.founderEmail}` link and the destructive styling; do not touch the `useEffect`/`getCalApi` theme block or the success-path `<Cal>` render. In `book/page.tsx`, rewrite the `metadata.description` to drop the `siteConfig.founderName` interpolation and gendered pronoun — use anonymous phrasing ("a plain conversation about where your business is losing time and money to manual work — no pitch"); the body copy is already anonymous, leave it. The `siteConfig.founderName` constant itself stays in `site.ts` (internal-only). Then run both exit greps and resolve any remaining rendered-output hits: (1) `grep -ril "missed call\|intake triage\|dental\|home services\|law firm\|real estate" src public` must return zero; (2) `grep -rn "founderName\|Annie\|\bshe\b\|\bher\b" src` must have every hit be either removed from rendered output or a code-comment/internal-constant-only reference that never reaches the browser.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "Email us directly" src/components/cal-embed.tsx && ! grep -qi "founderName\|\bshe\b\|\bher\b" src/components/cal-embed.tsx && ! grep -qi "founderName" src/app/book/page.tsx && test -z "$(grep -ril 'missed call\|intake triage\|dental\|home services\|law firm\|real estate' src public 2>/dev/null)" && echo GREP_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cal-embed.tsx` fallback contains "Email us directly", keeps the `mailto:` link, and contains no `founderName`, `she`, or `her`.
    - `book/page.tsx` `metadata.description` no longer interpolates `siteConfig.founderName` and contains no gendered pronoun.
    - Exit grep 1 (v1 vocabulary across `src` and `public`) returns zero hits.
    - Exit grep 2 (`founderName`/`Annie`/`she`/`her` in `src`) has only the internal `site.ts` constant and/or code comments remaining — no rendered visitor-facing copy.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>All founder-name/pronoun references are removed from rendered copy and both LAND-06 exit greps pass across the fully-migrated repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← SSG HTML / metadata | Static routes, nav, metadata, and the Cal.com embed error fallback; no user input crosses in this plan |
| crawler ← sitemap/routes | Search crawlers read sitemap + resolve routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information disclosure | stale /about, /services routes in sitemap | mitigate | Delete route files AND trim `sitemap.ts` `routes` to `["", "/book"]` in the same plan so no sitemap entry points at a 404 |
| T-05-11 | Information disclosure | founder-name/pronoun in rendered copy | mitigate | Scrub Cal.com fallback and /book metadata; two exit greps gate that no name/pronoun reaches rendered output (anonymity constraint) |
| T-05-12 | Repudiation | surviving v1 positioning | mitigate | Scripted full-repo v1-vocabulary grep as a literal exit criterion (LAND-06), asserted after all migration is complete |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced |
</threat_model>

<verification>
- `npm run build` succeeds.
- `/about` and `/services` deleted; sitemap trimmed to `["", "/book"]`.
- Nav links repointed to `/#offer`, `/#process`, `/book`.
- Both anonymity/v1-vocabulary exit greps pass.
</verification>

<success_criteria>
LAND-06 fully satisfied: no v1 routes/copy/metadata survive, no founder-name/pronoun reaches rendered output, nav resolves to in-page anchors, build green.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-06-SUMMARY.md` when done
</output>
