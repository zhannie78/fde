---
phase: 05-fde-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/outcomes.tsx
  - src/components/sections/offer.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-05]
must_haves:
  truths:
    - "Visitor sees outcomes labeled with TIME, EFFICIENCY, and PROFIT vocabulary"
    - "Visitor sees an explicit standalone worst-case/conservative line ('Even in the worst case, you come out ahead')"
    - "Visitor sees scope-qualified pricing: free audit -> one-time setup under $10k -> retainer under $2k/mo"
    - "Visitor can click a Book Your Free Audit Call CTA mid-page (in the Offer section)"
  artifacts:
    - path: "src/components/sections/outcomes.tsx"
      provides: "TIME/EFFICIENCY/PROFIT outcomes with asymmetric lead+pair layout and worst-case line, id=outcomes"
      contains: "come out ahead"
    - path: "src/components/sections/offer.tsx"
      provides: "Scope-qualified numbered pricing sequence + mid BookCta, id=offer"
      contains: "id=\"offer\""
  key_links:
    - from: "src/components/sections/offer.tsx"
      to: "@/components/book-cta"
      via: "BookCta import (mid CTA)"
      pattern: "BookCta"
    - from: "src/components/sections/offer.tsx"
      to: "scope-qualifying language"
      via: "pricing tied to free audit"
      pattern: "scoped during your free audit"
---

<objective>
Rewrite Outcomes to the TIME/EFFICIENCY/PROFIT vocabulary with an explicit worst-case line (LAND-05), and create the Offer section carrying scope-qualified pricing (LAND-02) with the mid-page CTA (LAND-03). Both are Server Components built standalone; page.tsx is integrated in wave 2.

Purpose: Deliver the "outcomes" and "offer" parts of the LAND-01 hierarchy plus the conservative framing and mid CTA.
Output: Rewritten `outcomes.tsx`, new `offer.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Outcomes to TIME/EFFICIENCY/PROFIT with worst-case line</name>
  <files>src/components/sections/outcomes.tsx</files>
  <read_first>
    - src/components/sections/outcomes.tsx (current structure — keep the asymmetric lead + secondary-pair grid; replace labels/stats/body)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (Worst-case framing section; Color: lead outcome stat numeral uses text-primary)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (outcomes.tsx pattern assignment — exact grid classes to preserve, SITE-06 anti-pattern gate against a uniform 3-card grid)
  </read_first>
  <action>
    Rewrite the `outcomes` const array's three items to the locked vocabulary — TIME (hours recovered from manual work), EFFICIENCY (workflows that run themselves), PROFIT (money recovered or newly earned). Keep the existing asymmetric layout exactly: `const [lead, ...rest] = outcomes;`, the `lg:grid-cols-12` split with lead at `lg:col-span-7` and the pair at `lg:col-span-5` with `lg:border-l`. Do NOT convert to a uniform `grid-cols-3` card grid (explicit anti-pattern gate). Keep the lead stat numeral wrapped in `<span className="text-primary">` (accent). Add `id="outcomes"` to the section wrapper (currently absent). After the grid, add a standalone worst-case/conservative line carrying this exact framing: "Even in the worst case, you come out ahead." Represent EFFICIENCY qualitatively (it has no natural raw number). Keep outcomes vocabulary concrete/outcome-first (per RESEARCH Pattern 3, avoid drifting into capability-first "agentic/AI-native" language in this section). No lucide icons, no new token values.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "come out ahead" src/components/sections/outcomes.tsx && grep -q 'id="outcomes"' src/components/sections/outcomes.tsx && grep -qi "TIME\|EFFICIENCY\|PROFIT" src/components/sections/outcomes.tsx && ! grep -q "grid-cols-3" src/components/sections/outcomes.tsx && echo OUTCOMES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `outcomes.tsx` compiles and exports `Outcomes`.
    - File contains the exact string `Even in the worst case, you come out ahead` (or the exact substring `come out ahead`).
    - File contains `id="outcomes"` and the three vocabulary words TIME, EFFICIENCY, PROFIT.
    - File retains the asymmetric `lg:col-span-7` / `lg:col-span-5` layout and does NOT contain `grid-cols-3`.
    - Lead stat numeral uses `text-primary`.
  </acceptance_criteria>
  <done>Outcomes renders TIME/EFFICIENCY/PROFIT on the preserved asymmetric layout, with an explicit standalone worst-case line and id=outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Offer section with scope-qualified pricing and mid CTA</name>
  <files>src/components/sections/offer.tsx</files>
  <read_first>
    - src/components/service-sequence.tsx (exact structural analog: numbered/asymmetric pricing sequence, step-1 widest + accent price)
    - src/components/book-cta.tsx (mid CTA reused verbatim)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (Pricing copy section — scope-qualifying language is mandatory, Pitfall 4)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (offer.tsx pattern assignment — exact layout/classes to copy)
  </read_first>
  <action>
    Create `src/components/sections/offer.tsx` exporting Server Component `Offer`. Copy the numbered/asymmetric sequence layout from `service-sequence.tsx`: an `<ol>` with `md:grid md:grid-cols-[1.6fr_1fr_1fr]` so step 1 is widest; each item shows a `number` (`01`/`02`/`03`), a `price`, a `title`, and body copy. Three steps: (01) Free Audit — price `$0`, "no cost, no commitment"; (02) One-Time Setup — price `Under $10k`; (03) Monthly Retainer — price `Under $2k/mo`. Step-1 price/numeral uses accent `text-primary` (index === 0 branch); other prices use `text-foreground`. Every price MUST ship scope-qualified and tied to the free audit — include the phrase "scoped during your free audit" (never a bare unconditional number as a standalone headline). Wrap in `<section id="offer" className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 sm:py-20">`. After the `<ol>`, render the mid CTA: import `BookCta` from `@/components/book-cta` and render `<BookCta />` (2nd of 3 placements, LAND-03). No uniform 3-card grid, no icons, no new tokens.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q 'id="offer"' src/components/sections/offer.tsx && grep -q "scoped during your free audit" src/components/sections/offer.tsx && grep -q "Under \$10k" src/components/sections/offer.tsx && grep -q "Under \$2k" src/components/sections/offer.tsx && grep -q "BookCta" src/components/sections/offer.tsx && echo OFFER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `offer.tsx` exists, exports `Offer`, compiles.
    - File contains `id="offer"`, the strings `Under $10k`, `Under $2k`, `$0`, and the scope-qualifier `scoped during your free audit`.
    - File imports and renders `BookCta` from `@/components/book-cta`.
    - File uses the `md:grid-cols-[1.6fr_1fr_1fr]` asymmetric layout (not a uniform `grid-cols-3`).
    - Step-1 price/numeral uses `text-primary`.
  </acceptance_criteria>
  <done>Offer renders the free-audit -> under-$10k -> under-$2k/mo sequence with mandatory scope-qualifying language, asymmetric layout, and a mid-page BookCta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← SSG HTML | Static pricing/outcomes copy; no user input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Repudiation | Offer pricing copy | mitigate | Prices ship scope-qualified and tied to the free audit ("scoped during your free audit") so no unconditional public price commitment is made (Pitfall 4) |
| T-05-03 | Information disclosure | Outcomes/Offer static copy | accept | No PII/secrets; React default-escaped text, no `dangerouslySetInnerHTML` |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced |
</threat_model>

<verification>
- `npm run build` succeeds.
- Outcomes uses TIME/EFFICIENCY/PROFIT + worst-case line, asymmetric layout preserved.
- Offer shows scope-qualified pricing and a mid BookCta.
</verification>

<success_criteria>
Outcomes and Offer sections built on current tokens, LAND-02/LAND-05 copy requirements met, mid CTA present, no uniform card grid, build green.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-02-SUMMARY.md` when done
</output>
