---
phase: 05-fde-landing-page
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/process-transparency.tsx
  - src/components/sections/final-cta.tsx
autonomous: true
requirements: [LAND-01, LAND-03, LAND-04]
must_haves:
  truths:
    - "Visitor sees a step-by-step process-transparency section (audit -> setup -> retainer) on the dark trust band"
    - "The process section no longer links to the deleted /services route"
    - "Visitor can click a Book Your Free Audit Call CTA at the bottom of the page (dark band)"
  artifacts:
    - path: "src/components/sections/process-transparency.tsx"
      provides: "Renamed/rewritten from engagement-flow: numbered process steps on bg-secondary dark band, id=process"
      contains: "id=\"process\""
    - path: "src/components/sections/final-cta.tsx"
      provides: "Bottom CTA on dark band, v1 copy removed"
      contains: "BookCta"
  key_links:
    - from: "src/components/sections/final-cta.tsx"
      to: "@/components/book-cta"
      via: "BookCta import (bottom CTA)"
      pattern: "BookCta"
    - from: "src/components/sections/process-transparency.tsx"
      to: "dark trust band"
      via: "bg-secondary wrapper"
      pattern: "bg-secondary"
---

<objective>
Create ProcessTransparency (LAND-04) as a NEW file adapting the existing engagement-flow numbered-flow pattern into a step-by-step "how the engagement works" trust section, and edit FinalCta copy to drop v1 vocabulary while keeping its dark-band bottom CTA. engagement-flow.tsx is left in place this wave (deleted in wave 2 when page.tsx switches imports).

Purpose: Deliver the process-transparency trust signal near the CTA plus the bottom CTA placement.
Output: New `process-transparency.tsx`, edited `final-cta.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProcessTransparency section (new file from engagement-flow pattern)</name>
  <files>src/components/sections/process-transparency.tsx</files>
  <read_first>
    - src/components/sections/engagement-flow.tsx (exact structure to copy: dark band, numbered flow, asymmetric step widths; note the /services Link to remove)
    - src/components/service-sequence.tsx (richer step copy to merge in — audit -> fixed-scope setup -> monthly retainer)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (ProcessTransparency row: dark band, LAND-04 trust signal, no founder-name trust signals)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (process-transparency.tsx pattern assignment — keep gap-10 step gap and bg-secondary treatment; remove the /services Link)
  </read_first>
  <action>
    Create `src/components/sections/process-transparency.tsx` (a NEW file — do NOT rename or delete `engagement-flow.tsx` in this plan; the old page.tsx still imports it until wave 2). Export a Server Component named `ProcessTransparency`. Copy the engagement-flow structure verbatim: outer `<section id="process" className="bg-secondary text-secondary-foreground">` (dark trust band), inner `max-w-6xl` container, an H2 "How the engagement works" using `font-heading text-2xl ... sm:text-[1.75rem]`, and an `<ol>` of numbered steps with asymmetric widths (step 1 `sm:w-[42%]`, steps 2-3 `sm:w-[29%]`) and `gap-10` step gaps. Steps describe the process step-by-step: (01) Free audit — how the audit works and what you get; (02) Build & deploy the custom solution (fixed-scope setup); (03) Ongoing monthly iteration/monitoring (retainer). Step numerals use accent `text-primary`. REMOVE the bottom `<Link href="/services">` present in engagement-flow (that route is deleted in wave 2) — either drop it entirely or repoint to the in-page anchor `#offer`. Do NOT reintroduce any founder-name/photo trust signal (anonymity constraint). No lucide icons, no new token values.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q 'id="process"' src/components/sections/process-transparency.tsx && grep -q "bg-secondary" src/components/sections/process-transparency.tsx && ! grep -q 'href="/services"' src/components/sections/process-transparency.tsx && ! grep -qi "Annie\|founderName" src/components/sections/process-transparency.tsx && echo PROCESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `process-transparency.tsx` exists, exports `ProcessTransparency`, compiles.
    - File contains `id="process"` and `bg-secondary` (dark trust band).
    - File uses the asymmetric numbered layout (contains `sm:w-[42%]` or an equivalent asymmetric width; NOT a uniform `grid-cols-3`).
    - File contains NO `href="/services"` link and NO founder-name/pronoun references.
    - `engagement-flow.tsx` still exists and is untouched (deleted in wave 2).
  </acceptance_criteria>
  <done>ProcessTransparency exists as a new dark-band Server Component with numbered audit->setup->retainer steps, no /services link, no founder identity signals.</done>
</task>

<task type="auto">
  <name>Task 2: Edit FinalCta copy to drop v1 vocabulary (bottom CTA)</name>
  <files>src/components/sections/final-cta.tsx</files>
  <read_first>
    - src/components/sections/final-cta.tsx (current structure — copy-only edit, keep the bg-secondary rounded band and BookCta)
    - src/components/book-cta.tsx (bottom CTA reused verbatim)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (final-cta.tsx pattern assignment — note: its sample shows `id="cta"` as if present, but the live file has NO id; add it)
  </read_first>
  <action>
    Edit `final-cta.tsx` copy and add the missing section anchor. (1) ADD `id="cta"` to the `<section>` wrapper — the file currently has NO `id` attribute (verified), so this is a required addition, not a preserved value (the anchor is needed so the ProcessTransparency/nav flow and any future deep-link resolve; it also aligns with the other sections' id convention). (2) Rewrite the two copy strings (the `<h2>` "Ready to see what's slipping through?" and the `<p>`) to drop the v1 "what's slipping through / missed-call" framing and speak the FDE offer ("book the free audit" close). Keep the layout, the `bg-secondary` rounded band, `sm:py-24`, and the `<BookCta />` usage identical (this is the 3rd of 3 CTA placements, LAND-03). Do not add icons or new tokens.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "BookCta" src/components/sections/final-cta.tsx && grep -q 'id="cta"' src/components/sections/final-cta.tsx && grep -q "bg-secondary" src/components/sections/final-cta.tsx && ! grep -qi "missed call\|slipping through" src/components/sections/final-cta.tsx && echo FINALCTA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `final-cta.tsx` still exports `FinalCta`, has `id="cta"` added to the `<section>`, keeps `bg-secondary`, and renders `<BookCta />`.
    - Copy contains no v1 vocabulary (`missed call`, `slipping through`, vertical names).
    - Structure/classes unchanged except the two copy strings and the added `id="cta"`.
  </acceptance_criteria>
  <done>FinalCta carries FDE-offer close copy with the bottom BookCta, structure unchanged, no v1 vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← SSG HTML | Static process/CTA copy; no user input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information disclosure | ProcessTransparency/FinalCta copy | accept | No PII/secrets; anonymity preserved (no founder name/photo); React default-escaped text |
| T-05-07 | Information disclosure | dead /services link | mitigate | Remove the `/services` link in ProcessTransparency so no CTA points at a route deleted in wave 2 (avoids a broken-looking 404 path) |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced |
</threat_model>

<verification>
- `npm run build` succeeds.
- ProcessTransparency exists (new file), dark band, numbered steps, no /services link, no founder identity.
- FinalCta copy is v1-free with bottom BookCta; engagement-flow.tsx left intact for wave 2.
</verification>

<success_criteria>
LAND-04 process-transparency section built and the bottom CTA placement (LAND-03) delivered on current tokens, no v1 vocabulary, no anonymity violation, build green.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-04-SUMMARY.md` when done
</output>
