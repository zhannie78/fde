---
phase: 05-fde-landing-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/roi-calculator.tsx
autonomous: true
requirements: [PROOF-02, PROOF-03]
must_haves:
  truths:
    - "Visitor can enter hours/week and hourly cost into two numeric inputs with no signup and no network call"
    - "Visitor sees annual hours recovered and annual dollars recovered derived client-side"
    - "Visitor sees an honest empty state (not $0/0 hours) when hours/week is 0"
    - "Calculator output speaks TIME, EFFICIENCY, and PROFIT and terminates in a Book Your Free Audit Call CTA"
  artifacts:
    - path: "src/components/sections/roi-calculator.tsx"
      provides: "Client-island ROI calculator, useState only, no network, ends in BookCta, id=calculator"
      contains: "use client"
  key_links:
    - from: "src/components/sections/roi-calculator.tsx"
      to: "@/components/book-cta"
      via: "BookCta import (post-result CTA)"
      pattern: "BookCta"
    - from: "src/components/sections/roi-calculator.tsx"
      to: "client-side arithmetic"
      via: "WEEKS_PER_YEAR derivation, no fetch"
      pattern: "WEEKS_PER_YEAR"
---

<objective>
Build the client-side ROI calculator (PROOF-02/PROOF-03): a `"use client"` island with two numeric inputs deriving annual hours and dollars recovered via plain arithmetic, TIME/EFFICIENCY/PROFIT-framed output, an honest empty state, and a terminal BookCta. Zero network, zero persistence, zero new dependencies. Built standalone; page.tsx integrates it in wave 2.

Purpose: Deliver the interactive proof point that reinforces the outcomes vocabulary and routes back to the audit CTA.
Output: New `roi-calculator.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the RoiCalculator client-island component</name>
  <files>src/components/sections/roi-calculator.tsx</files>
  <read_first>
    - src/components/cal-embed.tsx (the only other "use client" component — copy its small-boundary discipline)
    - src/components/book-cta.tsx (terminal CTA reused verbatim)
    - .planning/phases/05-fde-landing-page/05-RESEARCH.md (Pattern 2 — the implementation baseline, WEEKS_PER_YEAR=50, clamp idiom)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (ROI Calculator Contract: fields/defaults/validation; empty-state and output copy; result numerals use text-primary)
  </read_first>
  <action>
    Create `src/components/sections/roi-calculator.tsx` starting with `"use client";` and exporting `RoiCalculator`. Use `useState` only — no react-hook-form, no zod, no fetch, no persistence. Two controlled `<input type="number" min={0}>` fields: `hoursPerWeek` (default `10`, label "Hours/week lost to manual work") and `hourlyCost` (default `35`, label "Your team's hourly cost"). On every change, clamp with `Math.max(0, Number(e.target.value) || 0)` to prevent NaN/negatives. Declare `const WEEKS_PER_YEAR = 50;` (deliberately not 52 — conservative, reinforces LAND-05 inside the calculator). Derive `annualHours = hoursPerWeek * WEEKS_PER_YEAR` and `annualDollars = annualHours * hourlyCost`. Wrap in `<section id="calculator" className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 sm:py-20">`. When `hoursPerWeek === 0`, render the empty state (heading "See what you're leaving on the table" and body "Enter your team's hours and hourly cost above — we'll show you the time and money a forward-deployed engineer could recover.") instead of a `$0`/`0 hours` result. Otherwise render output speaking all three outcomes: TIME ("{annualHours.toLocaleString()} hours/year back"), PROFIT ("roughly ${annualDollars.toLocaleString()} in recovered time"), and EFFICIENCY qualitatively ("even before counting the efficiency gains from work that no longer needs a person at all"). Result numerals (annual hours / annual dollars) use `text-primary` via `<strong>`. End the section with a terminal `<BookCta />` imported from `@/components/book-cta`. Style inputs with plain Tailwind (bare `<input>` is acceptable per UI-SPEC; if styled inputs are wanted, `npx shadcn@latest add input label` adds official-registry source files). No lucide icons, no new token values.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && head -1 src/components/sections/roi-calculator.tsx | grep -q "use client" && grep -q "WEEKS_PER_YEAR = 50" src/components/sections/roi-calculator.tsx && grep -q "hoursPerWeek === 0" src/components/sections/roi-calculator.tsx && grep -q "BookCta" src/components/sections/roi-calculator.tsx && ! grep -qi "fetch(\|useEffect\|react-hook-form\|from \"zod\"" src/components/sections/roi-calculator.tsx && echo ROI_OK</automated>
  </verify>
  <acceptance_criteria>
    - First line of `roi-calculator.tsx` is `"use client";`.
    - File exports `RoiCalculator`, compiles, and contains `WEEKS_PER_YEAR = 50`.
    - File contains the clamp idiom `Math.max(0,` on both input change handlers.
    - File branches on `hoursPerWeek === 0` to render the empty state (contains "See what you're leaving on the table").
    - Output copy contains "hours/year" (TIME), "recovered time" (PROFIT), and "efficiency" (EFFICIENCY); result numerals use `text-primary`.
    - File imports and renders `BookCta`; contains no `fetch(`, no `useEffect`, no `react-hook-form`, no `zod` import.
  </acceptance_criteria>
  <done>RoiCalculator is a self-contained client island: two clamped numeric inputs, conservative 50-week derivation, honest empty state, TIME/EFFICIENCY/PROFIT output with accent numerals, and a terminal BookCta — no network, no new deps.</done>
</task>

<task type="auto">
  <name>Task 2: Harden input validation and mobile touch targets</name>
  <files>src/components/sections/roi-calculator.tsx</files>
  <read_first>
    - src/components/sections/roi-calculator.tsx (the component from Task 1)
    - src/components/book-cta.tsx (h-11 touch-target idiom in the sticky variant)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (Spacing Scale: 44px minimum touch target = h-11 for calculator inputs on mobile; V5 input validation)
  </read_first>
  <action>
    Harden the calculator: ensure each `<input>` carries an associated accessible label (wrap in `<label>` or use `htmlFor`/`id` pairing) and a minimum 44px touch target on mobile via `h-11` (same idiom as `book-cta.tsx`'s sticky variant). Confirm the clamp already prevents NaN, negatives, and empty-string coercion (`Math.max(0, Number(value) || 0)`), and that extremely large inputs still render via `toLocaleString()` without crashing. Do not add any network call, persistence, or new dependency. This is the V5 input-validation and DoS-resilience mitigation for the client island.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "h-11" src/components/sections/roi-calculator.tsx && grep -c "Math.max(0" src/components/sections/roi-calculator.tsx | grep -qv "^0$" && echo HARDEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each number input has an accessible label (`<label>` wrapping or `htmlFor`/`id`).
    - Inputs use `h-11` (44px min touch target) on mobile.
    - Both change handlers use `Math.max(0, Number(...) || 0)` (grep count for `Math.max(0` is ≥ 2).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Both inputs are labeled, meet the 44px touch target, and clamp to non-negative numbers, hardening the client island against NaN/DoS input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← user input | The only interactive input surface in the phase: two numeric fields, values never leave the browser (no network, no persistence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service (client-side) | RoiCalculator number inputs | mitigate | Clamp/coerce every change with `Math.max(0, Number(value) || 0)` so NaN/negatives never reach `toLocaleString()`; empty-state branch avoids broken `$0` render |
| T-05-05 | Tampering (XSS) | RoiCalculator output | mitigate | Values used only in arithmetic and `toLocaleString()`; rendered via React default-escaped text, never `dangerouslySetInnerHTML`; no injection surface (values never leave the browser) |
| T-05-SC | Tampering | shadcn input/label (optional) | mitigate | If added, sourced from shadcn official registry via configured `components.json` (copy-paste source files, not new registry npm deps) — no legitimacy gate triggered |
</threat_model>

<verification>
- `npm run build` succeeds.
- Calculator is a `"use client"` island with clamped inputs, 50-week derivation, empty state, TIME/EFFICIENCY/PROFIT output, and terminal BookCta.
- No fetch/useEffect/react-hook-form/zod present.
</verification>

<success_criteria>
PROOF-02 (client-side, no signup, no API cost) and PROOF-03 (TIME/EFFICIENCY/PROFIT output routing to the audit CTA) satisfied by a hardened client island with zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-03-SUMMARY.md` when done
</output>
