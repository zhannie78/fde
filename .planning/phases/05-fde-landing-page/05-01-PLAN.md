---
phase: 05-fde-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/hero.tsx
  - src/components/sections/the-fix.tsx
autonomous: true
requirements: [LAND-01, LAND-03]
must_haves:
  truths:
    - "Visitor sees a gap-first Hero headline (not a missed-call/pain-point headline)"
    - "Visitor sees the 95% enterprise GenAI-failure stat cited to MIT NANDA, 2025, scoped to enterprise pilots"
    - "Visitor sees a 'the fix' section explaining forward-deployed engineering (embedded, workflow-first, white-glove)"
    - "Visitor can click a Book Your Free Audit Call CTA at the top of the page"
  artifacts:
    - path: "src/components/sections/hero.tsx"
      provides: "Gap headline + cited 95% stat line + top BookCta, single-column (DemoPlaceholder removed)"
      contains: "MIT NANDA"
    - path: "src/components/sections/the-fix.tsx"
      provides: "Forward-deployed engineering explainer section, id=fix"
      contains: "id=\"fix\""
  key_links:
    - from: "src/components/sections/hero.tsx"
      to: "@/components/book-cta"
      via: "BookCta import (top CTA)"
      pattern: "BookCta"
    - from: "src/components/sections/the-fix.tsx"
      to: "accent eyebrow label idiom"
      via: "text-primary uppercase label"
      pattern: "text-primary uppercase"
---

<objective>
Rewrite the Hero to carry the gap (95%-failure framing, cited) and create TheFix section (forward-deployed engineering explainer) — the first two of the five narrative parts. These are Server Components built standalone against the existing section skeletons; page.tsx is NOT touched in this plan (integrated in wave 2).

Purpose: Deliver the "gap" and "fix" halves of the LAND-01 hierarchy and the top-of-page CTA (LAND-03) using current design tokens only.
Output: Rewritten `hero.tsx`, new `the-fix.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Hero to gap-first framing with cited 95% stat and top CTA</name>
  <files>src/components/sections/hero.tsx</files>
  <read_first>
    - src/components/sections/hero.tsx (current v1 structure and copy to replace)
    - src/components/sections/demo-placeholder.tsx (the import being removed — confirm no other coupling)
    - src/components/book-cta.tsx (the CTA component reused verbatim — do not duplicate its Link/string)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (95%-stat citation section; Typography: Display reserved for Hero H1 only; Color contract)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (hero.tsx pattern assignment: keep layout skeleton, replace copy)
  </read_first>
  <action>
    Replace the Hero's v1 copy with gap-first FDE framing. Keep the existing `Hero` named export and the section skeleton (Display-size H1 via `style={{ fontSize: "clamp(32px, 8vw, 44px)" }}`, `font-heading leading-[1.1] font-semibold text-foreground`; body `text-base leading-[1.6]`). The H1 states the gap between AI capability and real business implementation (e.g. a "powerful model is not the same thing as a working system" framing, rephrased — do not quote fde.academy verbatim). Directly beneath the headline/body, render the cited stat as a small line using `text-sm text-muted-foreground` with this scope-accurate wording: "95% of enterprise generative-AI pilots fail to show measurable ROI (MIT NANDA, 2025)." Follow with body copy bridging to SMBs (same brittle-workflow problem, worse for SMBs who can't afford enterprise FDE). Remove the `DemoPlaceholder` import and its right-column usage; collapse the layout to single-column (drop the `lg:flex-row` / `lg:w-1/2` two-column split, keep content in a `max-w` container). Render the top CTA by importing `BookCta` from `@/components/book-cta` and rendering `<BookCta />` (this is the top of 3 CTA placements, LAND-03). Do NOT introduce lucide icons, motion, or any new token values. Do NOT use Display size anywhere except this H1.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q "MIT NANDA" src/components/sections/hero.tsx && grep -q "BookCta" src/components/sections/hero.tsx && ! grep -q "DemoPlaceholder" src/components/sections/hero.tsx && echo HERO_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/hero.tsx` exports `Hero` and compiles (`npm run build` succeeds).
    - File contains the exact source string `MIT NANDA` and the word `enterprise` in the citation line.
    - File imports and renders `BookCta` from `@/components/book-cta`; no hardcoded `<Link href="/book">` or duplicated "Book Your Free Audit Call" string.
    - File contains no reference to `DemoPlaceholder` and no `demo-placeholder` import.
    - File contains no v1 vocabulary (`missed call`, `intake triage`, vertical names).
    - Display size (`clamp(32px, 8vw, 44px)`) appears exactly once (the H1).
  </acceptance_criteria>
  <done>Hero renders a gap-first headline, the MIT-NANDA-cited 95% enterprise stat line, SMB bridge copy, and a top `BookCta`, single-column, with no DemoPlaceholder reference and no v1 vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Create TheFix forward-deployed-engineering explainer section</name>
  <files>src/components/sections/the-fix.tsx</files>
  <read_first>
    - src/components/sections/outcomes.tsx (structural analog: asymmetric static explainer, eyebrow-label idiom)
    - src/components/sections/engagement-flow.tsx (heading-role sizing reference: text-2xl / sm:text-[1.75rem])
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (the-fix.tsx pattern assignment — exact wrapper/eyebrow/heading idiom, no icon grid)
    - .planning/phases/05-fde-landing-page/05-RESEARCH.md (Architecture Pattern 3 — fde.academy-derived framing language for the fix, adapted to solo/anonymous SMB scale)
  </read_first>
  <action>
    Create `src/components/sections/the-fix.tsx` exporting a Server Component named `TheFix`. Wrap in `<section id="fix" className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 sm:py-20">`. Lead with an uppercase accent eyebrow label using `text-sm font-semibold tracking-[0.02em] text-primary uppercase` (e.g. "The Fix"). Follow with a heading using `mt-2 font-heading text-2xl leading-[1.2] font-semibold text-foreground sm:text-[1.75rem]` and body copy `mt-4 max-w-xl text-base leading-[1.6] text-foreground`. Copy explains forward-deployed engineering as embedded, workflow-first, white-glove: engineers embed directly in the client's real workflows and own the AI system end-to-end (adapt the "still on the hook when it breaks" ownership framing from RESEARCH Pattern 3; do NOT cite enterprise case studies — John Deere/Fox/JPMC/AWS/Databricks — per RESEARCH anti-pattern). Use buyer vocabulary ("forward-deployed engineer", "automation", "AI agents") naturally, not stuffed. No lucide icons. No new token values. No Display-size text.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -q 'id="fix"' src/components/sections/the-fix.tsx && grep -q "TheFix" src/components/sections/the-fix.tsx && ! grep -qi "John Deere\|JPMC\|Databricks" src/components/sections/the-fix.tsx && echo FIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/the-fix.tsx` exists, exports `TheFix`, and compiles.
    - File contains `id="fix"` on the section wrapper.
    - File contains an uppercase accent eyebrow using `text-primary uppercase`.
    - File contains no lucide-react import and no enterprise case-study names (John Deere, Fox, JPMC, AWS, Databricks).
    - File is a Server Component (no `"use client"` directive).
  </acceptance_criteria>
  <done>TheFix section exists as a Server Component with id=fix, an accent eyebrow, and forward-deployed-engineering explainer copy using buyer vocabulary, no icons, no enterprise name-drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← SSG HTML | Static server-rendered marketing copy; no user input crosses into this plan's components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | Hero/TheFix static copy | accept | No PII, no secrets, no dynamic data — pure marketing copy rendered via React default-escaped text (no `dangerouslySetInnerHTML`) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced in this plan; nothing to install |
</threat_model>

<verification>
- `npm run build` succeeds.
- Hero cites the 95% stat as MIT NANDA / enterprise-scoped; renders a top `BookCta`; single-column, no DemoPlaceholder.
- TheFix exists with id=fix and forward-deployed-engineering copy.
</verification>

<success_criteria>
Gap (Hero) and Fix (TheFix) sections are built as Server Components on current tokens, top CTA present, no v1 vocabulary, no enterprise name-drops, build green.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-01-SUMMARY.md` when done
</output>
