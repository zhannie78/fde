---
phase: 05-fde-landing-page
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - src/app/page.tsx
  - src/components/sections/verticals-teaser.tsx
  - src/components/sections/founder-strip.tsx
  - src/components/sections/demo-placeholder.tsx
  - src/components/sections/engagement-flow.tsx
autonomous: true
requirements: [LAND-01, LAND-03, LAND-06]
must_haves:
  truths:
    - "Visiting / renders the narrative in exact order: Hero (gap) -> TheFix (fix) -> Outcomes -> RoiCalculator -> Offer -> ProcessTransparency -> FinalCta (CTA)"
    - "The v1 section components (verticals teaser, founder strip, demo placeholder, engagement-flow) no longer exist"
    - "Book Your Free Audit Call CTAs are reachable at top (Hero), mid (Offer), and bottom (FinalCta) scroll depth"
    - "page.tsx carries gap-first metadata and no v1 vocabulary"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "New 7-section composition in locked order + gap-first metadata"
      contains: "ProcessTransparency"
  key_links:
    - from: "src/app/page.tsx"
      to: "the seven section components"
      via: "import + ordered render"
      pattern: "RoiCalculator"
---

<objective>
Integrate wave-1 sections into `page.tsx` in the locked order (LAND-01), and delete the now-orphaned v1 section components that only `page.tsx`/`hero.tsx` imported. This is the composition wave — after it, `/` renders the full FDE narrative and the build resolves against the new components.

Purpose: Wire the narrative together in the testable order and remove the first tranche of v1 positioning artifacts.
Output: Rewritten `page.tsx`; deleted `verticals-teaser.tsx`, `founder-strip.tsx`, `demo-placeholder.tsx`, `engagement-flow.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fde-landing-page/05-UI-SPEC.md
@.planning/phases/05-fde-landing-page/05-PATTERNS.md
@.planning/phases/05-fde-landing-page/05-01-SUMMARY.md
@.planning/phases/05-fde-landing-page/05-02-SUMMARY.md
@.planning/phases/05-fde-landing-page/05-03-SUMMARY.md
@.planning/phases/05-fde-landing-page/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite page.tsx composition to the locked 7-section order</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current v1 composition + metadata to replace)
    - src/components/sections/hero.tsx (wave-1 output — Hero export)
    - src/components/sections/the-fix.tsx (wave-1 output — TheFix export)
    - src/components/sections/outcomes.tsx (wave-1 output — Outcomes export)
    - src/components/sections/roi-calculator.tsx (wave-1 output — RoiCalculator export)
    - src/components/sections/offer.tsx (wave-1 output — Offer export)
    - src/components/sections/process-transparency.tsx (wave-1 output — ProcessTransparency export)
    - src/components/sections/final-cta.tsx (wave-1 output — FinalCta export)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (page.tsx target composition — this is the target, not illustrative)
    - .planning/phases/05-fde-landing-page/05-UI-SPEC.md (Phase Component Inventory — section order is a testable success criterion, do NOT reorder)
  </read_first>
  <action>
    Rewrite `src/app/page.tsx` to import and render exactly these seven sections in this exact order (LAND-01, ROADMAP Success Criterion #1 — order is verified): `Hero`, `TheFix`, `Outcomes`, `RoiCalculator`, `Offer`, `ProcessTransparency`, `FinalCta`. Import `Hero` from `@/components/sections/hero`, `TheFix` from `@/components/sections/the-fix`, `Outcomes` from `@/components/sections/outcomes`, `RoiCalculator` from `@/components/sections/roi-calculator`, `Offer` from `@/components/sections/offer`, `ProcessTransparency` from `@/components/sections/process-transparency`, `FinalCta` from `@/components/sections/final-cta`. Remove all v1 imports (`EngagementFlow`, `FounderStrip`, `VerticalsTeaser`). Keep `page.tsx` a Server Component (the `"use client"` boundary stays inside `RoiCalculator` only). Rewrite the exported `metadata` (title + description) to gap-first FDE framing using buyer vocabulary (forward-deployed engineering, AI agents, automation) — remove any v1 "missed-call"/generic phrasing. Do not add new tokens or visual treatments.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/app/page.tsx','utf8'); const order=['Hero','TheFix','Outcomes','RoiCalculator','Offer','ProcessTransparency','FinalCta']; const idx=order.map(n=>s.indexOf('<'+n)); if(idx.some(i=>i<0)) throw new Error('missing section'); for(let i=1;i<idx.length;i++){if(idx[i]<idx[i-1]) throw new Error('out of order at '+order[i]);} console.log('ORDER_OK');" && ! grep -qi "missed call\|VerticalsTeaser\|FounderStrip\|EngagementFlow" src/app/page.tsx && echo PAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - `page.tsx` renders `<Hero>`, `<TheFix>`, `<Outcomes>`, `<RoiCalculator>`, `<Offer>`, `<ProcessTransparency>`, `<FinalCta>` in that exact source order (the node ORDER_OK check passes).
    - `page.tsx` contains no `EngagementFlow`, `FounderStrip`, `VerticalsTeaser` imports and no v1 vocabulary.
    - `page.tsx` remains a Server Component (no `"use client"`).
    - `metadata` title/description use FDE/buyer vocabulary, not v1 phrasing.
  </acceptance_criteria>
  <done>/ composes the seven sections in the locked order with gap-first metadata and no v1 imports; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the orphaned v1 section components</name>
  <files>src/components/sections/verticals-teaser.tsx, src/components/sections/founder-strip.tsx, src/components/sections/demo-placeholder.tsx, src/components/sections/engagement-flow.tsx</files>
  <read_first>
    - src/app/page.tsx (confirm Task 1 removed all imports of these files first)
    - src/components/sections/hero.tsx (confirm wave-1 removed the demo-placeholder import)
    - .planning/phases/05-fde-landing-page/05-PATTERNS.md (Deleted table — reasons: 4-vertical scrapped, anonymity, scrapped demo, renamed to process-transparency)
  </read_first>
  <action>
    Delete these four now-orphaned v1 section files: `src/components/sections/verticals-teaser.tsx` (4-vertical strategy scrapped), `src/components/sections/founder-strip.tsx` (named-founder trust signal violates anonymity), `src/components/sections/demo-placeholder.tsx` (tied to scrapped missed-call demo), and `src/components/sections/engagement-flow.tsx` (superseded by `process-transparency.tsx` created in wave 1). Before deleting, confirm nothing still imports them (Task 1 removed the page.tsx imports; hero.tsx dropped demo-placeholder in wave 1). Use file deletion (e.g. `git rm`/`rm`), not emptying the files.
  </action>
  <verify>
    <automated>! test -f src/components/sections/verticals-teaser.tsx && ! test -f src/components/sections/founder-strip.tsx && ! test -f src/components/sections/demo-placeholder.tsx && ! test -f src/components/sections/engagement-flow.tsx && ! grep -rn "verticals-teaser\|founder-strip\|demo-placeholder\|engagement-flow" src --include="*.tsx" --include="*.ts" && npm run build 2>&1 | tail -5 && echo DELETE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The four files no longer exist on disk.
    - No remaining import references to `verticals-teaser`, `founder-strip`, `demo-placeholder`, or `engagement-flow` anywhere under `src`.
    - `npm run build` succeeds after deletion.
  </acceptance_criteria>
  <done>All four orphaned v1 section components are deleted with no dangling imports; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← SSG HTML | Static composition; no user input crosses in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information disclosure | leftover v1 components in bundle | mitigate | Delete the orphaned v1 section files so no retired positioning ships in the client bundle or source |
| T-05-09 | Tampering (build integrity) | page.tsx composition | mitigate | `npm run build` gates the integration; ordered-section node check prevents silently shipping a reordered/incomplete narrative |
| T-05-SC | Tampering | npm installs | mitigate | No new dependencies introduced |
</threat_model>

<verification>
- `npm run build` succeeds.
- Ordered-section node check passes (7 sections, locked order).
- Four v1 section files deleted, no dangling imports, no v1 vocabulary in page.tsx.
</verification>

<success_criteria>
LAND-01 order wired and verified, LAND-03 CTAs present at top/mid/bottom via the composed sections, first tranche of LAND-06 v1 artifacts removed, build green.
</success_criteria>

<output>
Create `.planning/phases/05-fde-landing-page/05-05-SUMMARY.md` when done
</output>
