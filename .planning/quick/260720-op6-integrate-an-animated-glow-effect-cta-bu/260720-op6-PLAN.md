---
phase: quick-260720-op6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/glow.tsx
  - src/components/book-cta.tsx
  - src/components/sections/hero.tsx
  - src/components/sections/final-cta.tsx
autonomous: true
requirements: [OP6-GLOW-CTA]
must_haves:
  truths:
    - "The Hero 'Book Your Free Audit Call' button renders with an animated green-duotone glow behind it"
    - "The FinalCta 'Book Your Free Audit Call' button renders with the same animated glow"
    - "Header, mobile-nav, footer, offer, and roi-calculator BookCta instances render unchanged (no glow)"
    - "The sticky mobile BookCta variant keeps its full-width h-11 sizing"
    - "npm run build passes"
  artifacts:
    - path: "src/components/ui/glow.tsx"
      provides: "GlowButton client component wrapping the project Button in a motion-animated glow layer"
      contains: "export function GlowButton"
    - path: "src/components/book-cta.tsx"
      provides: "BookCta with optional glow prop that switches rendering to GlowButton"
      contains: "glow"
  key_links:
    - from: "src/components/book-cta.tsx"
      to: "src/components/ui/glow.tsx"
      via: "import { GlowButton }"
      pattern: "GlowButton"
    - from: "src/components/sections/hero.tsx"
      to: "src/components/book-cta.tsx"
      via: "<BookCta glow />"
      pattern: "BookCta glow"
    - from: "src/components/sections/final-cta.tsx"
      to: "src/components/book-cta.tsx"
      via: "<BookCta glow />"
      pattern: "BookCta glow"
---

<objective>
Integrate an animated glow-effect CTA button into the live codebase and apply it to the primary
"Book Your Free Audit Call" button (BookCta) at its two highest-impact placements: Hero and FinalCta.

Purpose: Draw the eye to the site's single conversion action without introducing a second accent
hue — the glow uses a monochromatic duotone derived from the live signal-green (`#1F6E4A`) to
respect the existing single-accent-color design discipline.

Output: A new `src/components/ui/glow.tsx` (GlowButton), a `glow?: boolean` prop on BookCta, and
the two highest-impact call sites opted in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@src/components/book-cta.tsx
@src/components/ui/button.tsx

<interfaces>
<!-- Existing Button/buttonVariants export shape — GlowButton must match this exactly. -->

From src/components/ui/button.tsx:
```typescript
// Uses the UNIFIED radix-ui package: import { Slot } from "radix-ui"  (NOT @radix-ui/react-slot)
function Button(props: React.ComponentProps<"button"> &
  VariantProps<typeof buttonVariants> & { asChild?: boolean }): JSX.Element
export { Button, buttonVariants }
// variant: default | outline | secondary | ghost | destructive | link
// size:    default | xs | sm | lg | icon | icon-xs | icon-sm | icon-lg
```

From src/components/book-cta.tsx (current — do NOT lose the sticky logic):
```typescript
type BookCtaProps = { variant?: "primary" | "sticky"; className?: string };
// Renders <Button asChild size={sticky?"lg":"default"} className={cn(sticky && "h-11 w-full text-base", className)}>
//   <Link href="/book">Book Your Free Audit Call</Link>
// </Button>
```

Live design token (src/app/globals.css line 59): `--primary: #1F6E4A;` (deep signal green, CTA-only).
Glow duotone: `["#1F6E4A", "#3DBD82"]` — do NOT introduce a second hue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion and create the GlowButton component</name>
  <files>package.json, package-lock.json, src/components/ui/glow.tsx</files>
  <action>
Run `npm install motion` (the npm package formerly known as framer-motion — it is NOT yet installed).
Do NOT install `hugeicons-react`, `@radix-ui/react-slot`, or any other package — the unified
`radix-ui` package already satisfies Slot, and no icon is needed.

Create `src/components/ui/glow.tsx` with the exact adapted GlowButton source below (ready to paste —
do not re-derive it). It is a `"use client"` component importing `motion` from `"motion/react"`,
`Button` and `buttonVariants` from `@/components/ui/button`, and `cn` from `@/lib/utils`. Defaults are
intentionally the restrained green duotone `["#1F6E4A", "#3DBD82"]` in `"breathe"` mode — this is the
correct adaptation for this project's single-accent discipline; do NOT revert to a 4-color rotate default.

The file content is the GlowButton source supplied in the task ground-truth (fact #5): the
`GlowEffectLayer` internal component with BLUR_PRESETS, blurClass, the mode-keyed animations map, and
the exported `GlowButton` wrapper that layers a motion glow div behind the project `Button` with
`variant`/`size`/`asChild`/`disabled` passthrough and `whileHover` spring scale. Export both a named
`GlowButton` and a default export.
  </action>
  <verify>
    <automated>test -f src/components/ui/glow.tsx && grep -q "export function GlowButton" src/components/ui/glow.tsx && grep -q '"motion"' package.json && npx tsc --noEmit</automated>
  </verify>
  <done>`motion` is in package.json dependencies; `src/components/ui/glow.tsx` exists, exports `GlowButton`, imports Button/buttonVariants/cn from this project's paths, and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add glow prop to BookCta and opt in Hero + FinalCta</name>
  <files>src/components/book-cta.tsx, src/components/sections/hero.tsx, src/components/sections/final-cta.tsx</files>
  <action>
In `src/components/book-cta.tsx`: add an optional `glow?: boolean` field to `BookCtaProps`
(default undefined/false). When `glow` is true, render via `<GlowButton asChild ...>` (import
`GlowButton` from `@/components/ui/glow`) instead of the plain `<Button asChild>`; when false/undefined,
keep rendering the existing plain `<Button asChild>` exactly as today. In BOTH branches preserve the
unchanged `size={variant === "sticky" ? "lg" : "default"}`, the
`cn(variant === "sticky" && "h-11 w-full text-base", className)` className logic, and the
`<Link href="/book">Book Your Free Audit Call</Link>` children. GlowButton accepts the same
`asChild`/`size`/`className` props as Button, so the branch differs only in the wrapping component name.
Do NOT change the header, mobile-nav, footer, offer, or roi-calculator behavior — the default (no glow)
path must be byte-for-byte equivalent in rendered output to today.

Then set `glow` to true at ONLY the two highest-impact call sites:
- `src/components/sections/hero.tsx` line 36: change `<BookCta />` to `<BookCta glow />`
- `src/components/sections/final-cta.tsx` line 22: change `<BookCta />` to `<BookCta glow />`

Leave every other `<BookCta ... />` call site (header, mobile-nav, footer, offer, roi-calculator)
exactly as-is — plain, non-glowing.
  </action>
  <verify>
    <automated>grep -q "glow" src/components/book-cta.tsx && grep -q "GlowButton" src/components/book-cta.tsx && grep -q "BookCta glow" src/components/sections/hero.tsx && grep -q "BookCta glow" src/components/sections/final-cta.tsx && npm run build</automated>
  </verify>
  <done>BookCta accepts `glow`; when true it renders GlowButton, otherwise the unchanged plain Button. Hero and FinalCta pass `glow`; no other call site changed. `npm run build` passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports no type errors (glow.tsx and book-cta.tsx typecheck).
- `npm run build` completes successfully (Next.js 16 App Router production build).
- `grep -rn "BookCta glow" src/components/sections` returns exactly two matches (hero + final-cta).
- `grep -rn "<BookCta" src/components/header* src/components/*mobile-nav* src/components/sections/footer.tsx src/components/sections/offer.tsx src/components/sections/roi-calculator.tsx 2>/dev/null` shows no `glow` attribute on those call sites.
</verification>

<success_criteria>
- `motion` installed; `src/components/ui/glow.tsx` created with the adapted green-duotone GlowButton.
- BookCta gains an opt-in `glow?: boolean` prop that is false-safe (existing call sites unchanged).
- Hero and FinalCta CTAs render with the animated glow; all five other BookCta placements unchanged.
- The sticky mobile variant retains its `h-11 w-full text-base` sizing in both glow and non-glow paths.
- `npm run build` passes.
</success_criteria>

<output>
Create `.planning/quick/260720-op6-integrate-an-animated-glow-effect-cta-bu/260720-op6-SUMMARY.md` when done.
</output>
