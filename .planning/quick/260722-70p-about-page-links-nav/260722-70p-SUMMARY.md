---
status: complete
---

# Quick Task 260722-70p: About-page links + nav reorder — Summary

**Plan:** `260722-70p-PLAN.md`
**Tasks:** 3/3 complete, no deviations

## Commits

- `a206d79`: feat(quick-260722-70p): link four hero phrases to /about
- `bea1489`: refactor(quick-260722-70p): reorder nav so About precedes Approach
- `12225c0`: feat(quick-260722-70p): link founder name in contact/book H1 to /about

## Changes

1. **Hero phrases** (`src/components/sections/hero.tsx`) — "Forward Deployed Engineer", "AI Consultant", "save time", and "increase profit" now wrap in unstyled `next/link` `Link`s to `/about` (`className="text-inherit no-underline"`), nested inside their existing `<span className="text-primary">` / `<strong className="lede-highlight">` wrappers so visual appearance is unchanged (no color/hover/underline change).
2. **Nav reorder** (`src/components/site-header.tsx`, `src/components/mobile-nav.tsx`, `src/components/site-footer.tsx`) — About now sits directly before Approach in header, mobile nav, and footer (footer order: Home, About, Approach, Pricing, Process). Reorder only — no hrefs, labels, or styles touched.
3. **Contact/Book headings** (`src/app/contact/page.tsx`, `src/app/book/page.tsx`) — H1s now read "Get in Touch with Annie" / "Book Your Free Audit Call with Annie", with the name sourced from `siteConfig.founderName` (not hardcoded, per D-06/D-13) and linked to `/about` with the same unstyled-link treatment.

## Verification

- `npx tsc --noEmit` passes clean.
- All task-level automated verify commands passed (run from worktree root — path-equivalent to the plan's specified `cd` target).
- Manual spot-check deferred to post-merge (see plan's `<verification>` section: load `/`, `/contact`, `/book`, confirm no visual link styling and correct nav order).

## Issues Encountered

None. Executed exactly as planned.
