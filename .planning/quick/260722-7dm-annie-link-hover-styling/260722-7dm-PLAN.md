---
phase: quick-260722-7dm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contact/page.tsx
  - src/app/book/page.tsx
  - src/components/sections/hero.tsx
autonomous: true
requirements: [STYLE-7dm]
must_haves:
  truths:
    - "The Annie link on /contact and /book is underlined and turns blue (--primary #3552FF) on hover"
    - "The 'Annie' hero link is a plain underline with blue hover, no boxShadow accent underline"
    - "The four other hero links (Forward Deployed Engineer, AI Consultant, save time, increase profit) are unchanged"
    - "The /contact H1 colors the entire phrase 'Get in Touch' with text-primary"
    - "The /book H1 heading color scope is unchanged (only 'Free Audit Call' colored)"
  artifacts:
    - path: "src/app/contact/page.tsx"
      provides: "Underlined blue-hover Annie link + 'Get in Touch' fully colored H1"
    - path: "src/app/book/page.tsx"
      provides: "Underlined blue-hover Annie link"
    - path: "src/components/sections/hero.tsx"
      provides: "Plain underline blue-hover Annie link, other four links untouched"
  key_links: []
---

<objective>
Follow-up styling adjustment to the /about links from quick task 260722-70p. Three exact, pre-specified changes:

1. Make the "Annie" link on /contact and /book underlined with a blue hover color.
2. Replace the hero "Hello, I'm Annie" link's boxShadow accent-underline with a plain underline + blue-hover treatment, leaving the four other hero links untouched.
3. On /contact only, expand the H1 color span from just "Touch" to the full "Get in Touch" phrase.

Purpose: Consistent link-hover affordance across the Annie /about links; tighten the contact page heading color scope.
Output: Three edited files, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

The `--primary` / `text-primary` token is `#3552FF` (indigo-blue) — this is the intended "blue". `hover:text-primary` is a valid Tailwind utility already used in this codebase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Annie links (contact, book, hero) and expand contact H1 color scope</name>
  <files>src/app/contact/page.tsx, src/app/book/page.tsx, src/components/sections/hero.tsx</files>
  <action>
Make these four exact edits. Do not touch anything else in these files.

1. src/app/contact/page.tsx — Annie link (line ~24): change className from `text-inherit no-underline` to `underline hover:text-primary transition-colors`. Final: `<Link href="/about" className="underline hover:text-primary transition-colors">`.

2. src/app/contact/page.tsx — H1 (line ~23): expand the color span from wrapping only "Touch" to wrapping the full "Get in Touch" phrase. Change `Get in <span className="text-primary">Touch</span> with{" "}` to `<span className="text-primary">Get in Touch</span> with{" "}`. Leave the trailing `with{" "}` and the Annie link exactly as-is (aside from edit #1).

3. src/app/book/page.tsx — Annie link (line ~25): change className from `text-inherit no-underline` to `underline hover:text-primary transition-colors`. Final: `<Link href="/about" className="underline hover:text-primary transition-colors">`. Do NOT change the book H1 color scope — `Book Your <span className="text-primary">Free Audit Call</span> with{" "}` stays exactly as-is.

4. src/components/sections/hero.tsx — the "Hello, I'm Annie" link (lines ~30-36): remove the `style={{ boxShadow: "inset 0 -2px 0 0 var(--accent)" }}` inline style entirely, and change className from `font-normal text-foreground` to `font-normal text-foreground underline hover:text-primary transition-colors`. Final link: `<Link href="/about" className="font-normal text-foreground underline hover:text-primary transition-colors">`. Do NOT modify the four other hero /about links (Forward Deployed Engineer at line ~49, AI Consultant at line ~54, save time at line ~64, increase profit at line ~70) — each must remain `className="text-inherit no-underline"` with no hover treatment. This is a hard constraint.
  </action>
  <verify>
    <automated>cd /Users/annie/Documents/Claude/fde && grep -q 'underline hover:text-primary transition-colors' src/app/contact/page.tsx && grep -q 'underline hover:text-primary transition-colors' src/app/book/page.tsx && grep -q 'font-normal text-foreground underline hover:text-primary transition-colors' src/components/sections/hero.tsx && grep -q '<span className="text-primary">Get in Touch</span>' src/app/contact/page.tsx && ! grep -q 'boxShadow' src/components/sections/hero.tsx && [ "$(grep -c 'text-inherit no-underline' src/components/sections/hero.tsx)" = "4" ] && echo OK</automated>
  </verify>
  <done>
- /contact and /book Annie links use `underline hover:text-primary transition-colors` (no `text-inherit no-underline`).
- /contact H1 wraps the full "Get in Touch" in `text-primary`.
- /book H1 color scope unchanged.
- Hero Annie link has no boxShadow and uses `font-normal text-foreground underline hover:text-primary transition-colors`.
- Exactly four `text-inherit no-underline` links remain in hero.tsx.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` (or the project's typecheck) passes — edits are className/style only, no type impact.
- Manual/visual (optional): the Annie link on /contact, /book, and the hero underlines and turns #3552FF on hover; the four other hero links are visually unchanged.
</verification>

<success_criteria>
All four edits applied exactly as specified; the four non-Annie hero links and the book H1 color scope are untouched.
</success_criteria>

<output>
Create `.planning/quick/260722-7dm-annie-link-hover-styling/260722-7dm-SUMMARY.md` when done.
</output>
