---
phase: quick-260724-cq6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/site-header.tsx
  - src/components/mobile-nav.tsx
  - src/components/site-footer.tsx
autonomous: true
requirements: [CQ6-01]
must_haves:
  truths:
    - "A 'Blog' link appears in the desktop header nav on every non-/about route"
    - "A 'Blog' link appears in the mobile hamburger sheet nav"
    - "A 'Blog' link appears in the footer nav"
    - "All three Blog links navigate to /blog"
    - "No existing nav item is restyled, reordered, or removed"
  artifacts:
    - path: "src/components/site-header.tsx"
      provides: "Desktop nav Blog link"
      contains: "/blog"
    - path: "src/components/mobile-nav.tsx"
      provides: "Mobile nav Blog link"
      contains: "/blog"
    - path: "src/components/site-footer.tsx"
      provides: "Footer nav Blog link"
      contains: "/blog"
  key_links:
    - from: "src/components/site-header.tsx"
      to: "/blog route"
      via: "navLinks array entry"
      pattern: "href: \"/blog\""
    - from: "src/components/site-footer.tsx"
      to: "/blog route"
      via: "footerLinks array entry"
      pattern: "href: \"/blog\""
---

<objective>
Add a "Blog" navigation link to the site header (desktop), mobile hamburger
menu, and footer so the newly-added /blog section is reachable from site
navigation.

Purpose: The /blog route (listing + individual posts) already exists but is
orphaned — nothing links to it. This wires it into the primary nav surfaces.
Output: Three edited component files, each gaining one Blog link that reuses
the existing nav conventions verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The three nav components. Each defines a plain link array that is .map()-ed
# with existing styling — the ONLY change is adding one entry per array.
@src/components/site-header.tsx
@src/components/mobile-nav.tsx
@src/components/site-footer.tsx

<interfaces>
<!-- Existing link-array shape used by all three components. Reuse exactly. -->

site-header.tsx (desktop) and mobile-nav.tsx both define:
```typescript
const navLinks = [
  { href: "/about", label: "About" },
  { href: "/#fix", label: "Approach" },
  { href: "/#offer", label: "Pricing" },
  { href: "/#process", label: "Process" },
];
```

site-footer.tsx defines:
```typescript
const footerLinks = [
  { href: "/", label: "Home" },
  { href: "/about", label: "About" },
  { href: "/#fix", label: "Approach" },
  { href: "/#offer", label: "Pricing" },
  { href: "/#process", label: "Process" },
];
```

Each array is rendered via .map() with pre-existing className styling. Adding
`{ href: "/blog", label: "Blog" }` is the entire change — the map handles
markup and styling automatically. No JSX, no className, no component edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Blog" link to header, mobile, and footer nav arrays</name>
  <files>src/components/site-header.tsx, src/components/mobile-nav.tsx, src/components/site-footer.tsx</files>
  <action>
Add a single link entry `{ href: "/blog", label: "Blog" }` to each of the three
existing link arrays, appended as the LAST item so no existing item is
reordered:

1. src/components/site-header.tsx — append to the `navLinks` array (currently
   ends with the Process entry). This flows through the desktop
   NavigationMenu .map() and picks up the existing active-state /
   border-b hover styling automatically.

2. src/components/mobile-nav.tsx — append to its `navLinks` array (same order,
   ends with Process). This flows through the SheetClose-wrapped Link .map() so
   the mobile hamburger sheet gains the Blog link with existing styling.

3. src/components/site-footer.tsx — append to the `footerLinks` array (currently
   ends with the Process entry). This flows through the footer nav .map() with
   existing muted-hover styling.

Do NOT edit any JSX, className, markup, or the .map() bodies. Do NOT reorder or
restyle existing entries. Do NOT create new components — the existing
array-driven pattern already handles reuse. The change is exactly three
one-line array additions (satisfies CQ6-01).
  </action>
  <verify>
    <automated>grep -c 'href: "/blog"' src/components/site-header.tsx src/components/mobile-nav.tsx src/components/site-footer.tsx | grep -v ':0$' | grep -c ':1$' | grep -qx 3 && npx tsc --noEmit</automated>
  </verify>
  <done>All three components contain a `{ href: "/blog", label: "Blog" }` entry, TypeScript compiles clean, and no existing nav entry was changed.</done>
</task>

</tasks>

<verification>
- Run the dev/build and confirm the Blog link renders in: desktop header (on a
  non-/about route such as `/`), the mobile hamburger sheet, and the footer.
- Clicking each Blog link navigates to `/blog`.
- Existing nav items (About, Approach, Pricing, Process; footer also Home)
  remain in their original order with unchanged styling.
</verification>

<success_criteria>
- "Blog" link present and functional in header, mobile nav, and footer.
- All three point to `/blog`.
- Zero changes to existing nav item order, markup, or styling.
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/quick/260724-cq6-add-blog-link-to-header-and-footer-navig/260724-cq6-SUMMARY.md` when done
</output>
